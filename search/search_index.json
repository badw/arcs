{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"ARCS","text":"<p>Automated Reactions for CO<sub>2</sub> Storage</p> <p>ARCS is a Python toolkit for exploring the network of chemical reactions that can occur among impurities in stored CO<sub>2</sub>. It turns first-principles thermochemistry into a weighted reaction graph, samples that graph with a Monte\u00a0Carlo random walk, and reports which reactions dominate and how species concentrations evolve.</p> <p>Under development</p> <p>These DOCS are under development </p>"},{"location":"#what-it-does","title":"What it does","text":"<p>Given a set of coupled-cluster (or DFT) energies and vibrational frequencies for each molecule, plus a list of candidate reactions, ARCS:</p> <ol> <li>Computes the Gibbs free energy of reaction and equilibrium constant for    every reaction at a chosen temperature and pressure.</li> <li>Builds a weighted, directed reaction graph (<code>networkx.MultiDiGraph</code>) where    compounds and reactions are nodes, and edge weights come from a thermodynamic    cost function.</li> <li>Samples the graph many times over, starting from an initial gas mixture,    using equilibrium chemistry at each step to update concentrations.</li> <li>Analyses the ensemble of runs to rank reactions and summarise how each    compound's concentration changes.</li> </ol>"},{"location":"#the-pipeline-at-a-glance","title":"The pipeline at a glance","text":"<pre><code>graph LR\n    A[Quantum data&lt;br/&gt;energies + vibrations] --&gt; B[GraphGenerator]\n    B --&gt; C[Reaction graph&lt;br/&gt;MultiDiGraph]\n    C --&gt; D[Traversal.sample]\n    E[GenerateInitialConcentrations] --&gt; D\n    D --&gt; F[AnalyseSampling]\n    F --&gt; G[Reaction statistics&lt;br/&gt;+ average concentrations]</code></pre>"},{"location":"#where-to-go-next","title":"Where to go next","text":""},{"location":"#citation-and-credits","title":"Citation and credits","text":"<p>ARCS was funded by and carried out in collaboration with Equinor, TotalEnergies, and Shell.</p> <p>The author is Benjamin A. D. Williamson (<code>benjamin.williamson@ntnu.no</code>). ARCS is released under the MIT licence.</p>"},{"location":"installation/","title":"Installation","text":""},{"location":"installation/#requirements","title":"Requirements","text":"<p>ARCS requires Python 3.10 or newer and builds on a number of scientific packages (ASE, pymatgen, chempy, NetworkX, SciPy, NumPy, pandas, and others). These are installed automatically as dependencies.</p>"},{"location":"installation/#from-source","title":"From source","text":"<p>The recommended way to install the development version is directly from the repository:</p> <pre><code>git clone https://github.com/badw/arcs.git\ncd arcs\npip install .\n</code></pre> <p>To install in editable mode while developing:</p> <pre><code>pip install -e .\n</code></pre>"},{"location":"installation/#backend-data","title":"Backend data","text":"<p>ARCS ships with coupled-cluster reference data in <code>src/arcs/data/quantum_data.json.gz</code>. Computed using Psi4 </p> <p>This file contains the per-molecule energies and vibrational frequencies used to build reaction graphs. </p> <p>The list of included compounds and the calculation settings are documented in <code>src/arcs/data/README.md</code>.</p>"},{"location":"quickstart/","title":"Quickstart","text":"<p>This page walks through a complete ARCS run: building a reaction graph from the bundled quantum data, seeding an initial gas mixture, sampling the network, and analysing the outcome.</p>"},{"location":"quickstart/#1-build-the-reaction-graph","title":"1. Build the reaction graph","text":"<p><code>GraphGenerator</code> reads the quantum data, evaluates the thermodynamics of every reaction at your chosen conditions, and returns a weighted <code>networkx.MultiDiGraph</code>.</p> <pre><code>from arcs.generate import GraphGenerator\n\ngg = GraphGenerator()\ngraph = gg.from_file(\n    filename=\"src/arcs/data/quantum_data.json.gz\",\n    temperature=248,          # Kelvin\n    pressure=20,              # bar\n    max_reaction_length=4,    # max reactants + products per reaction\n)\n</code></pre>"},{"location":"quickstart/#2-set-the-initial-concentrations","title":"2. Set the initial concentrations","text":"<p><code>GenerateInitialConcentrations</code> produces a concentration dictionary keyed by every compound in the graph. <code>update_ic</code> starts from an all-zero baseline and overrides just the species you name (values are in ppm-like units).</p> <pre><code>from arcs.generate import GenerateInitialConcentrations\n\nconcentrations = GenerateInitialConcentrations(graph=graph).update_ic(\n    {\"H2O\": 30, \"O2\": 10, \"SO2\": 10, \"H2S\": 10, \"NO2\": 10}\n)\n</code></pre>"},{"location":"quickstart/#3-sample-the-network","title":"3. Sample the network","text":"<p><code>Traversal</code> performs many independent random walks over the graph. Each walk repeatedly picks likely compounds, ranks the reactions that connect them, solves the resulting equilibrium with <code>chempy</code>, and updates the concentrations. Sampling is parallelised across CPUs.</p> <pre><code>from arcs.traversal import Traversal\n\nt = Traversal(graph=graph)\n\nresults = t.sample(\n    initial_concentrations=concentrations,\n    nsamples=1000,   # number of independent random walks\n    ncpus=4,         # parallel workers\n)\n</code></pre> <p><code>results</code> is a list with one entry per sample. Each entry holds the <code>concentrations</code> at every step and the <code>reaction_statistics</code> for the reactions that fired.</p>"},{"location":"quickstart/#4-analyse-the-results","title":"4. Analyse the results","text":"<p><code>AnalyseSampling</code> aggregates the ensemble.</p>"},{"location":"quickstart/#reaction-frequencies","title":"Reaction frequencies","text":"<p>Which reactions occurred most often across all samples:</p> <pre><code>import pandas as pd\nfrom arcs.analysis import AnalyseSampling\n\nanalysis = AnalyseSampling()\nstats = pd.Series(analysis.reaction_statistics(results)).sort_values(ascending=False)\nstats.head(10)\n</code></pre> <pre><code>1 H2 + 1 SO2 = 1 O2 + 1 H2S              369\n1 H2O + 1 SO2 = 1 H2SO3                  270\n2 H2 + 1 O2 = 2 H2O                      227\n3 O2 + 2 H2S = 2 H2O + 2 SO2             163\n...\n</code></pre>"},{"location":"quickstart/#average-concentration-changes","title":"Average concentration changes","text":"<p>How each species shifted, on average, from its initial value:</p> <pre><code>average_data = pd.DataFrame(analysis.average_sampling(results))\naverage_data = average_data.loc[~(average_data == 0).all(axis=1)]\naverage_data.sort_values(by=\"diff\", inplace=True)\naverage_data.round(2)\n</code></pre> <pre><code>compound  initial  mean  diff  sem   std   var\nH2S        10.0    4.88 -5.12  0.10  4.75  22.53\nNO2        10.0    6.19 -3.81  0.10  4.85  23.48\nO2         10.0    6.24 -3.76  0.12  5.76  33.18\n...\n</code></pre>"},{"location":"quickstart/#visualise-the-reaction-network","title":"Visualise the reaction network","text":"<p><code>result_to_pyvis</code> builds an interactive pyvis network of the most frequent reactions, coloured by frequency:</p> <pre><code>net = analysis.result_to_pyvis(results, head=10, cmap=\"Reds\")\nnet.show(\"reactions.html\")\n</code></pre> <p>This produces a standalone HTML file you can open in a browser. Here is a live example of the kind of network it generates \u2014 drag the nodes, scroll to zoom, and hover a reaction to see its equation:</p>"},{"location":"quickstart/#next-steps","title":"Next steps","text":"<ul> <li>How ARCS works explains the model behind each step.</li> <li>Sampling the network covers the <code>Traversal</code> parameters that   control how walks explore the graph.</li> <li>The API Reference documents every option in detail.</li> </ul>"},{"location":"api/analysis/","title":"<code>arcs.analysis</code>","text":"<p>Aggregation and visualisation of sampling results.</p>"},{"location":"api/analysis/#arcs.analysis.AnalyseSampling","title":"AnalyseSampling","text":"<pre><code>AnalyseSampling(\n    use_markdown: bool = False, use_latex: bool = False\n)\n</code></pre> Source code in <code>src/arcs/analysis.py</code> <pre><code>def __init__(\n    self,\n    use_markdown: bool = False,\n    use_latex: bool = False,\n):\n\n    if use_markdown and use_latex:\n        warnings.warn(\n            \"both use_markdown and use_latex are set to True, proceeding with use_markdown=True\"\n        )\n    self.use_markdown = use_markdown\n    self.use_latex = use_latex\n</code></pre>"},{"location":"api/analysis/#arcs.analysis.AnalyseSampling.use_markdown","title":"use_markdown  <code>instance-attribute</code>","text":"<pre><code>use_markdown = use_markdown\n</code></pre>"},{"location":"api/analysis/#arcs.analysis.AnalyseSampling.use_latex","title":"use_latex  <code>instance-attribute</code>","text":"<pre><code>use_latex = use_latex\n</code></pre>"},{"location":"api/analysis/#arcs.analysis.AnalyseSampling.latex_equation","title":"latex_equation  <code>staticmethod</code>","text":"<pre><code>latex_equation(\n    equation: str, use_markdown: bool = True\n) -&gt; str\n</code></pre> Source code in <code>src/arcs/analysis.py</code> <pre><code>@staticmethod\ndef latex_equation(\n    equation: str,\n    use_markdown: bool = True,\n) -&gt; str:\n    r, p = equation.split(\"=\")\n    reacs = r.split(\" \")\n    prods = p.split(\" \")\n\n    def _latex_format(reaction_elements):\n        reacs_adjusted = []\n        for i in reaction_elements:\n            try:\n                int(i)\n                reacs_adjusted.append(i)\n            except Exception:\n                if i == \"+\":\n                    reacs_adjusted.append(\" + \")\n                else:\n                    new_i = []\n                    for x in i:\n                        try:\n                            x = int(x)\n                            if use_markdown:\n                                new_i.append(\"&lt;sub&gt;{}&lt;/sub&gt;\".format(x))\n                            else:\n                                new_i.append(\"$_{}$\".format(x))\n                        except Exception:\n                            new_i.append(x)\n                    reacs_adjusted.append(\"\".join(new_i))\n        return \"\".join(reacs_adjusted)\n\n    rs = _latex_format(reacs)\n    ps = _latex_format(prods)\n\n    return \"\".join([rs, \" = \", ps])\n</code></pre>"},{"location":"api/analysis/#arcs.analysis.AnalyseSampling.sci_notation","title":"sci_notation  <code>staticmethod</code>","text":"<pre><code>sci_notation(number, sig_fig=2)\n</code></pre> Source code in <code>src/arcs/analysis.py</code> <pre><code>@staticmethod\ndef sci_notation(number, sig_fig=2):\n    ret_string = \"{0:.{1:d}e}\".format(number, sig_fig)\n    a, b = ret_string.split(\"e\")\n    # remove leading \"+\" and strip leading zeros\n    b = int(b)\n    return a + \" * 10^\" + str(b)\n</code></pre>"},{"location":"api/analysis/#arcs.analysis.AnalyseSampling.flip_reaction","title":"flip_reaction  <code>staticmethod</code>","text":"<pre><code>flip_reaction(reaction_data)\n</code></pre> Source code in <code>src/arcs/analysis.py</code> <pre><code>@staticmethod\ndef flip_reaction(reaction_data):\n    k = reaction_data[\"equilibrium_constant\"]\n    reaction_string = reaction_data[\"reaction\"][\"reaction_string\"]\n    reactants = reaction_data[\"reaction\"][\"reactants\"]\n    products = reaction_data[\"reaction\"][\"products\"]\n    if k &gt;= 1:\n        return reaction_data\n    else:\n        new_dict = {\"reactants\": products, \"products\": reactants}\n        r, p = reaction_string.split(\" = \")\n        new_dict[\"reaction_string\"] = f\"{p} = {r}\"\n        return {\"reaction\": new_dict, \"equilibrium_constant\": 1 / k}\n</code></pre>"},{"location":"api/analysis/#arcs.analysis.AnalyseSampling.reaction_statistics","title":"reaction_statistics","text":"<pre><code>reaction_statistics(\n    data: dict, flip_reaction: bool = True\n) -&gt; dict\n</code></pre> <p>function that takes the reaction statistics of each sample and combines them returns a dict</p> Source code in <code>src/arcs/analysis.py</code> <pre><code>def reaction_statistics(self, data: dict, flip_reaction: bool = True) -&gt; dict:\n    \"\"\"\n    function that takes the reaction statistics of each sample and combines them\n    returns a dict\n    \"\"\"\n    equations = []\n    for sample in data:\n        for equation in sample[\"reaction_statistics\"].values():\n            if equation:\n                if flip_reaction:\n                    equations.append(\n                        self.flip_reaction(equation)[\n                            \"reaction\"][\"reaction_string\"]\n                    )\n                else:\n                    equations.append(\n                        equation[\"reaction\"][\"reaction_string\"])\n\n    statistics = Counter(equations)\n\n    if self.use_markdown:\n        statistics = {\n            self.latex_equation(k, use_markdown=True): v\n            for k, v in statistics.items()\n        }\n    elif self.use_latex:\n        statistics = {\n            self.latex_equation(k, use_markdown=False): v\n            for k, v in statistics.items()\n        }\n\n    return statistics\n</code></pre>"},{"location":"api/analysis/#arcs.analysis.AnalyseSampling.average_sampling","title":"average_sampling  <code>staticmethod</code>","text":"<pre><code>average_sampling(data: dict) -&gt; dict\n</code></pre> <p>given a set of data, returns the initial,mean,diff and std</p> Source code in <code>src/arcs/analysis.py</code> <pre><code>@staticmethod\ndef average_sampling(\n    data: dict,\n) -&gt; dict:\n    \"\"\"given a set of data, returns the initial,mean,diff and std\"\"\"\n\n    average_data = {0: None, 1: []}\n    for sample in data:\n        for i, concentrations in sample[\"concentrations\"].items():\n            if int(i) == 0:\n                average_data[0] = concentrations\n            else:\n                average_data[1].append(concentrations)\n\n    import pandas as pd\n\n    df = pd.DataFrame(average_data[1])\n    df1 = pd.DataFrame(average_data[1]).mean(axis=0)\n    df0 = pd.Series(average_data[0])\n    df2 = df1 - df0\n    return pd.DataFrame(\n        [df0, df1, df2, df.sem(axis=0), df.std(axis=0), df.var(axis=0)],\n        index=[\"initial\", \"mean\", \"diff\", \"sem\", \"std\", \"var\"],\n    ).T.to_dict()\n</code></pre>"},{"location":"api/analysis/#arcs.analysis.AnalyseSampling.count_path_length","title":"count_path_length  <code>staticmethod</code>","text":"<pre><code>count_path_length(data: dict) -&gt; dict\n</code></pre> Source code in <code>src/arcs/analysis.py</code> <pre><code>@staticmethod\ndef count_path_length(\n    data: dict,\n) -&gt; dict:\n    return dict(Counter([len(x[\"concentrations\"]) for x in data[0:]]))\n</code></pre>"},{"location":"api/analysis/#arcs.analysis.AnalyseSampling.reduce_data_by_minimum_path_length","title":"reduce_data_by_minimum_path_length  <code>staticmethod</code>","text":"<pre><code>reduce_data_by_minimum_path_length(\n    data: dict, minimum_path_length: int = 1\n) -&gt; list\n</code></pre> Source code in <code>src/arcs/analysis.py</code> <pre><code>@staticmethod\ndef reduce_data_by_minimum_path_length(\n    data: dict, minimum_path_length: int = 1\n) -&gt; list:\n    new_data = [data[0]]\n    for _data in data[1:]:\n        if len(_data[\"concentrations\"]) &gt;= minimum_path_length:\n            new_data.append(_data)\n    return new_data\n</code></pre>"},{"location":"api/analysis/#arcs.analysis.AnalyseSampling.result_to_pyvis","title":"result_to_pyvis","text":"<pre><code>result_to_pyvis(\n    data: dict,\n    head: int = 10,\n    cmap: str = \"Reds\",\n    **pyvis_kwargs: dict\n) -&gt; Network\n</code></pre> Source code in <code>src/arcs/analysis.py</code> <pre><code>def result_to_pyvis(\n    self, data: dict, head: int = 10, cmap: str = \"Reds\", **pyvis_kwargs: dict\n) -&gt; pyvis.network.Network:\n\n    reaction_statistics = (\n        pd\n        .Series(self.reaction_statistics(data, flip_reaction=False))\n        .head(head)\n        .sort_values(ascending=False)\n        .to_dict()\n    )\n\n    reaction_data = {}\n    for sample in data:\n        for step in sample[\"reaction_statistics\"].values():\n            if step:\n                reaction, k = step.values()\n                reaction_data[reaction[\"reaction_string\"]] = {\n                    \"reaction\": reaction,\n                    \"equilibrium_constant\": k,\n                }\n\n    reaction_data = {\n        r: d for r, d in reaction_data.items() if r in list(reaction_statistics)\n    }\n\n    G = nx.MultiDiGraph(directed=True)\n    for i, (reaction_string, reaction_dict) in enumerate(reaction_data.items()):\n        reaction, k = reaction_dict.values()\n        if k &lt; 1:\n            G.add_weighted_edges_from(\n                [compound, str(i), 1] for compound in reaction[\"products\"]\n            )  # products -&gt; reaction\n            G.add_weighted_edges_from(\n                [str(i), compound, 1] for compound in reaction[\"reactants\"]\n            )  # reaction -&gt; products\n        else:\n            G.add_weighted_edges_from(\n                [compound, str(i), 1] for compound in reaction[\"reactants\"]\n            )  # products -&gt; reaction\n            G.add_weighted_edges_from(\n                [str(i), compound, 1] for compound in reaction[\"products\"]\n            )  # reaction -&gt; products\n\n    compound_nodes = []\n\n    reaction_nodes = []\n    for node in G.nodes():\n        try:\n            int(node)\n            reaction_nodes.append(node)\n        except Exception:\n            compound_nodes.append(node)\n    # reaction node formatting\n    # reaction node sizes = 8\n    reaction_node_sizes = {\n        str(i): 8 for i, r in enumerate(reaction_nodes)\n    }  # reaction['frequency']\n    nx.set_node_attributes(G, reaction_node_sizes, name=\"size\")\n    # reaction_colour set to frequency\n    # frequencies = pd.Series({str(i):reaction['frequency']  for i,reaction in rs.items()}) #\n    colour_map = mpl.colormaps[cmap]\n    norm = Normalize(\n        vmin=pd.Series(reaction_statistics).min(),\n        vmax=pd.Series(reaction_statistics).max(),\n    )\n    reaction_colours = {\n        str(i): rgb2hex(colour_map(norm(freq)))\n        for i, (r, freq) in enumerate(reaction_statistics.items())\n    }\n    nx.set_node_attributes(G, reaction_colours, name=\"color\")\n\n    g = Network(\n        **pyvis_kwargs\n        # height='750%',\n        # width='100%',\n        # notebook=False,\n        # directed=True,\n        # font_color='white',\n        # neighborhood_highlight=True\n    )\n\n    g.from_nx(G, edge_scaling=False)\n    for i, (reaction, reaction_dict) in enumerate(reaction_data.items()):\n        g.node_map[str(i)][\"shape\"] = \"circle\"\n        g.node_map[str(i)][\"title\"] = self.flip_reaction(reaction_dict)[\"reaction\"][\n            \"reaction_string\"\n        ]  # '{} ; frequency = {}'.format(\n        # reaction_dict['reaction']['reaction_string'],\n        # reaction_dict['reaction']['frequency']\n        # )\n        g.node_map[str(i)][\"label\"] = None\n\n    for compound in compound_nodes:\n        g.node_map[compound][\"shape\"] = \"box\"\n\n    # g.barnes_hut()\n\n    # g.save_graph(filename)\n\n    return g\n</code></pre>"},{"location":"api/errors/","title":"<code>arcs.errors</code>","text":"<p>Work in progress</p> <p><code>arcs.errors</code> computes error metrics by comparing the bundled quantum data against the Burcat thermochemical database. It is not yet wired into the main ARCS pipeline and its interface may change.</p>"},{"location":"api/errors/#arcs.errors.RMSEErrors","title":"RMSEErrors","text":"<pre><code>RMSEErrors(reaction_dict)\n</code></pre> Source code in <code>src/arcs/errors.py</code> <pre><code>def __init__(self, reaction_dict):\n    self.burcat_species = {'H2': 'H2  REF ELEMENT',\n                           'N2': 'N2  REF ELEMENT',\n                           'O2': 'O2 REF ELEMENT',\n                           'S8': 'S8',\n                           'H2O': 'H2O',\n                           'SO2': 'SO2',\n                           'SO3': 'SO3',\n                           'H2S': 'H2S',\n                           'CS2': 'CS2',\n                           'CO': 'CO',\n                           'COS': 'COS',\n                           'CO2': 'CO2',\n                           'HCN': 'HCN',\n                           'NO2': 'NO2',\n                           'NO': 'NO',\n                           'NO3': 'NO3',\n                           'N2O': 'N2O',\n                           'N2O4': 'N2O4',\n                           'O3': 'O3',\n                           'H2SO4': 'H2SO4',\n                           'HNO3': 'HNO3',\n                           'HNO2': 'HNO2',\n                           'CH3COOH': 'CH3COOH',\n                           'C2H5OH': 'C2H5OH',\n                           'C2H6O2': 'C2H6O2',\n                           'CH2O': 'CH2O',\n                           'CH3CHO': 'CH3CHO',\n                           'C6H6': 'C6H6',\n                           'C2H6': 'C2H6',\n                           'C3H8': 'C3H8',\n                           'C2H4': 'C2H4',\n                           'C4H8': 'C4H8',\n                           'H2O2': 'H2O2(L)',\n                           'N2O2': None,\n                           'CH4': 'CH4   ANHARMONIC',\n                           'NH3': 'NH3 Anharmonic',\n                           'H2SO3': None,\n                           'H2CO3': None,\n                           'HCOOH': 'HCOOH FORMIC ACID',\n                           'CH3OH': 'CH3OH(L)',\n                           'C6H14O4': None,\n                           'CH3NH2': 'CH5N',\n                           'C2H5NH2': None,\n                           'C3H7NH2': None,\n                           'CH3_2_CHCH2NH2': None,\n                           'C4H9NH2': None,\n                           'CH3(CO)CH3': 'C3H6O Acetone',\n                           'CH3_C6H5': 'C7H8  TOLUENE',\n                           'C2H5_C6H5': 'C8H10  C6H5C2H5',\n                           'C4H10': 'C4H10 n-butane',\n                           'C5H12': 'C5H12,i-pentane',\n                           'C6H14': 'C6H14,n-hexane',\n                           'C7H16': 'C7H16 n-heptane',\n                           'C8H18': 'C8H18,n-octane',\n                           'C3H6': 'C3H6 propylene'}\n\n    self.ev_conversion = physical_constants[\"Faraday constant\"][0] / 1000\n\n    self.burcatdb = Elementdb()\n\n    self.reaction_dict = reaction_dict\n</code></pre>"},{"location":"api/errors/#arcs.errors.RMSEErrors.burcat_species","title":"burcat_species  <code>instance-attribute</code>","text":"<pre><code>burcat_species = {\n    \"H2\": \"H2  REF ELEMENT\",\n    \"N2\": \"N2  REF ELEMENT\",\n    \"O2\": \"O2 REF ELEMENT\",\n    \"S8\": \"S8\",\n    \"H2O\": \"H2O\",\n    \"SO2\": \"SO2\",\n    \"SO3\": \"SO3\",\n    \"H2S\": \"H2S\",\n    \"CS2\": \"CS2\",\n    \"CO\": \"CO\",\n    \"COS\": \"COS\",\n    \"CO2\": \"CO2\",\n    \"HCN\": \"HCN\",\n    \"NO2\": \"NO2\",\n    \"NO\": \"NO\",\n    \"NO3\": \"NO3\",\n    \"N2O\": \"N2O\",\n    \"N2O4\": \"N2O4\",\n    \"O3\": \"O3\",\n    \"H2SO4\": \"H2SO4\",\n    \"HNO3\": \"HNO3\",\n    \"HNO2\": \"HNO2\",\n    \"CH3COOH\": \"CH3COOH\",\n    \"C2H5OH\": \"C2H5OH\",\n    \"C2H6O2\": \"C2H6O2\",\n    \"CH2O\": \"CH2O\",\n    \"CH3CHO\": \"CH3CHO\",\n    \"C6H6\": \"C6H6\",\n    \"C2H6\": \"C2H6\",\n    \"C3H8\": \"C3H8\",\n    \"C2H4\": \"C2H4\",\n    \"C4H8\": \"C4H8\",\n    \"H2O2\": \"H2O2(L)\",\n    \"N2O2\": None,\n    \"CH4\": \"CH4   ANHARMONIC\",\n    \"NH3\": \"NH3 Anharmonic\",\n    \"H2SO3\": None,\n    \"H2CO3\": None,\n    \"HCOOH\": \"HCOOH FORMIC ACID\",\n    \"CH3OH\": \"CH3OH(L)\",\n    \"C6H14O4\": None,\n    \"CH3NH2\": \"CH5N\",\n    \"C2H5NH2\": None,\n    \"C3H7NH2\": None,\n    \"CH3_2_CHCH2NH2\": None,\n    \"C4H9NH2\": None,\n    \"CH3(CO)CH3\": \"C3H6O Acetone\",\n    \"CH3_C6H5\": \"C7H8  TOLUENE\",\n    \"C2H5_C6H5\": \"C8H10  C6H5C2H5\",\n    \"C4H10\": \"C4H10 n-butane\",\n    \"C5H12\": \"C5H12,i-pentane\",\n    \"C6H14\": \"C6H14,n-hexane\",\n    \"C7H16\": \"C7H16 n-heptane\",\n    \"C8H18\": \"C8H18,n-octane\",\n    \"C3H6\": \"C3H6 propylene\",\n}\n</code></pre>"},{"location":"api/errors/#arcs.errors.RMSEErrors.ev_conversion","title":"ev_conversion  <code>instance-attribute</code>","text":"<pre><code>ev_conversion = (\n    physical_constants[\"Faraday constant\"][0] / 1000\n)\n</code></pre>"},{"location":"api/errors/#arcs.errors.RMSEErrors.burcatdb","title":"burcatdb  <code>instance-attribute</code>","text":"<pre><code>burcatdb = Elementdb()\n</code></pre>"},{"location":"api/errors/#arcs.errors.RMSEErrors.reaction_dict","title":"reaction_dict  <code>instance-attribute</code>","text":"<pre><code>reaction_dict = reaction_dict\n</code></pre>"},{"location":"api/errors/#arcs.errors.RMSEErrors.get_burcat_gibbs_free_energy_of_reaction","title":"get_burcat_gibbs_free_energy_of_reaction","text":"<pre><code>get_burcat_gibbs_free_energy_of_reaction(temperature)\n</code></pre> Source code in <code>src/arcs/errors.py</code> <pre><code>def get_burcat_gibbs_free_energy_of_reaction(self, temperature):\n\n    reactant_dict = self.reaction_dict[\"reactants\"]\n    sum_reactants = 0\n    for k, v in reactant_dict.items():\n        sum_reactants += (\n            self.burcatdb.getelementdata(\n                self.burcat_species[k]\n            ).go(T=temperature) / 1000 / self.ev_conversion\n        ) * v\n\n    product_dict = self.reaction_dict[\"products\"]\n    sum_products = 0\n    for k, v in product_dict.items():\n        sum_products += (\n            self.burcatdb.getelementdata(\n                self.burcat_species[k]\n            ).go(T=temperature) / 1000 / self.ev_conversion\n        ) * v\n\n    return sum_products - sum_reactants\n</code></pre>"},{"location":"api/errors/#arcs.errors.RMSEErrors.parse_reaction","title":"parse_reaction","text":"<pre><code>parse_reaction(reaction_dict)\n</code></pre> <p>probably should go in arcs.generate</p> Source code in <code>src/arcs/errors.py</code> <pre><code>def parse_reaction(self, reaction_dict):\n    \"probably should go in arcs.generate\"\n    species = Counter()\n    for d in list(reaction_dict[\"reactants\"]) + list(reaction_dict[\"products\"]):\n        species.update(parse_molecule(d))\n\n    return species\n</code></pre>"},{"location":"api/errors/#arcs.errors.RMSEErrors.rmse_reaction","title":"rmse_reaction","text":"<pre><code>rmse_reaction(parsed_reaction, rmse_element_wise)\n</code></pre> <p>probably should go in arcs.generate</p> Source code in <code>src/arcs/errors.py</code> <pre><code>def rmse_reaction(self, parsed_reaction, rmse_element_wise):\n    \"\"\"probably should go in arcs.generate\"\"\"\n    summed = []\n    for k, v in parsed_reaction.items():\n        summed.append(np.square(rmse_element_wise[k] * v))\n\n    return np.sqrt(np.mean(summed))\n</code></pre>"},{"location":"api/errors/#arcs.errors.RMSEErrors.get_element_rmse","title":"get_element_rmse","text":"<pre><code>get_element_rmse(quantum_reactions, burcat_reactions)\n</code></pre> <p>root median square error</p> Source code in <code>src/arcs/errors.py</code> <pre><code>def get_element_rmse(self, quantum_reactions, burcat_reactions):\n    \"root median square error\"\n\n    errors = defaultdict(dict)\n    for (q, qq), (b, bb), (r, rr) in zip(quantum_reactions.items(),     burcat_reactions.items(), self.reaction_dict.items()):\n        species = Counter()\n        # list(rr[\"reactants\"]) + list(rr[\"products\"]):\n        for d in rr[\"reactants\"]:  # only total species involved?\n            species.update(parse_molecule(d))\n\n        try:\n            errors[q][\"diff\"] = qq - bb\n            errors[q][\"species\"] = species\n        except Exception:\n            pass\n    # generate a dataframe\n    df = pd.DataFrame(errors).T\n    tmp = (\n        df['species']\n        .apply(pd.Series)\n        .stack()\n        .reset_index(level=1)\n        .rename(columns={'level_1': 'element', 0: 'count'})\n    )\n\n    tmp['diff'] = df.loc[tmp.index, 'diff'].values\n    tmp[\"diff_per_atom\"] = tmp[\"diff\"] / tmp[\"count\"]\n\n    tmp = tmp.dropna()\n\n    rmse = tmp.groupby('element').apply(\n        lambda g: np.sqrt(\n            np.median(\n                np.square(\n                    g['diff_per_atom'])\n            )\n        )\n    )\n\n    return rmse\n</code></pre>"},{"location":"api/generate/","title":"<code>arcs.generate</code>","text":"<p>The core module: it converts first-principles data into a weighted reaction graph and provides the initial-concentration helpers.</p>"},{"location":"api/generate/#graph-construction","title":"Graph construction","text":""},{"location":"api/generate/#arcs.generate.GraphGenerator","title":"GraphGenerator","text":"<pre><code>GraphGenerator()\n</code></pre> <p>generates an nx.multidigraph object with weightings from the dft data gibbs free energy and equilibrium constants.</p> <p>GraphGenerator</p> Source code in <code>src/arcs/generate.py</code> <pre><code>def __init__(self):\n    \"\"\"\n    GraphGenerator\n    \"\"\"\n    self.gibbs_filter = 709  # eV\n    self.quantum_dict_metadata = None\n</code></pre>"},{"location":"api/generate/#arcs.generate.GraphGenerator.gibbs_filter","title":"gibbs_filter  <code>instance-attribute</code>","text":"<pre><code>gibbs_filter = 709\n</code></pre>"},{"location":"api/generate/#arcs.generate.GraphGenerator.quantum_dict_metadata","title":"quantum_dict_metadata  <code>instance-attribute</code>","text":"<pre><code>quantum_dict_metadata = None\n</code></pre>"},{"location":"api/generate/#arcs.generate.GraphGenerator.cost_function","title":"cost_function","text":"<pre><code>cost_function(\n    gibbs_free_energy: float,\n    temperature: float,\n    reactants: dict,\n    normalise_by_reactant_atoms: bool = True,\n) -&gt; float\n</code></pre> <p>takes a gibbs free energy of reaction and normalises it using a cost function taken from: https://www.nature.com/articles/s41467-021-23339-x.pdf which takes the form: cost = ln(1+(273/temperature)*exp(G/num_reactant_atoms))</p> <p>(this is normalised per reactant atom)</p> <p>i.e. CO2 + H2 = H2O + CO</p> <p>num_reactant_atoms = 5</p> Source code in <code>src/arcs/generate.py</code> <pre><code>def cost_function(\n    self,\n    gibbs_free_energy: float,  # in eV\n    temperature: float,  # in K\n    reactants: dict,\n    normalise_by_reactant_atoms: bool = True\n) -&gt; float:\n    \"\"\"\n    takes a gibbs free energy of reaction and normalises it using a cost function taken from:\n    https://www.nature.com/articles/s41467-021-23339-x.pdf which takes the form:\n    cost = ln(1+(273/temperature)*exp(G/num_reactant_atoms))\n\n    (this is normalised per reactant atom)\n\n    i.e. CO2 + H2 = H2O + CO\n\n    num_reactant_atoms = 5\n\n    \"\"\"\n    if normalise_by_reactant_atoms:\n        compounds = []\n        for reactant, coefficient in reactants.items():\n            for i in range(coefficient):\n                compounds.append(reactant)\n\n        num_atoms = np.sum(\n            [\n                np.sum(\n                    [\n                        y for x, y in parse_molecule(compound).items()\n                    ]\n                )\n                for compound in compounds\n            ]\n        )\n    else:\n        num_atoms = 1\n\n    return (\n        np.log(\n            1 + (273 / temperature) * np.exp(gibbs_free_energy / num_atoms)\n        )\n    )\n</code></pre>"},{"location":"api/generate/#arcs.generate.GraphGenerator.generate_multidigraph","title":"generate_multidigraph","text":"<pre><code>generate_multidigraph(\n    temperature: float,\n    applied_reactions: list,\n    log_K: bool = False,\n) -&gt; multidigraph\n</code></pre> <p>This function generates reaction graph in networkx weighted using the self.costfunction returns an nx.multidigraph object</p> Source code in <code>src/arcs/generate.py</code> <pre><code>def generate_multidigraph(\n    self,\n    temperature: float,  # in K\n    applied_reactions: list,\n    log_K: bool = False,\n) -&gt; nx.multidigraph:\n    \"\"\"\n    This function generates reaction graph in networkx weighted using the self.costfunction\n    returns an nx.multidigraph object\n    \"\"\"\n\n    graph = nx.MultiDiGraph(directed=True)\n\n    for i, reaction in enumerate(applied_reactions):\n        forward_cost = self.cost_function(\n            gibbs_free_energy=reaction[\"g\"],\n            temperature=temperature,\n            reactants=reaction[\"r\"][\"reactants\"],\n        )\n        backward_cost = self.cost_function(\n            gibbs_free_energy=-reaction[\"g\"],\n            temperature=temperature,\n            reactants=reaction[\"r\"][\"products\"],\n        )\n\n        graph.add_weighted_edges_from(\n            [compound, i, forward_cost] for compound in reaction[\"r\"][\"reactants\"]\n        )  # reactants -&gt; reaction\n        graph.add_weighted_edges_from(\n            [i, compound, backward_cost] for compound in reaction[\"r\"][\"reactants\"]\n        )  # reaction -&gt; reactants\n        graph.add_weighted_edges_from(\n            [i, compound, forward_cost] for compound in reaction[\"r\"][\"products\"]\n        )  # reaction -&gt; products\n        graph.add_weighted_edges_from(\n            [compound, i, backward_cost] for compound in reaction[\"r\"][\"products\"]\n        )  # products -&gt; reaction\n\n    reactions = {i: r[\"r\"] for i, r in enumerate(applied_reactions)}\n\n    if log_K:\n        keys = [\"log_k\", \"log_equilibrium_constant\"]\n    else:\n        keys = [\"k\", \"equilibrium_constant\"]\n\n    equilibrium_constants = {i: r[keys[0]]\n                             for i, r in enumerate(applied_reactions)}\n\n    nx.set_node_attributes(graph, reactions, name=\"reaction\")\n    nx.set_node_attributes(\n        graph, equilibrium_constants, name=keys[1]\n    )\n\n    return graph\n</code></pre>"},{"location":"api/generate/#arcs.generate.GraphGenerator.from_file","title":"from_file","text":"<pre><code>from_file(\n    filename: str,\n    temperature: float,\n    pressure: float,\n    max_reaction_length: int = 5,\n    log_K: bool = False,\n    filter_large_gibbs: bool = True,\n) -&gt; MultiDiGraph\n</code></pre> <pre><code>generates a networkx.multidigraph from a .json file of reactions and quantum_dict generated with reactit and GetEnergyandVibrationsVASP\nneeds a sanity check on the file though\n\nfile takes the format of a dictionary with:\n{\n</code></pre> <p>'': {     'atoms': ase.Atoms.asdict(),     'pointgroup': ,     'spin': ,     'rotation_num': ,     'islinear': ,     'energy': ,     'vibrations': &lt;vibrations     } 'reactions':list({     'reaction_string':,                   'reactants':dict(reactants),                   'products':dict(products)}                   )     } <pre><code>reactions can be generated with https://github.com/badw/reactit.git\n</code></pre> Source code in <code>src/arcs/generate.py</code> <pre><code>def from_file(\n    self,\n    filename: str,\n    temperature: float,  # in K\n    pressure: float,  # in bar\n    max_reaction_length: int = 5,\n    log_K: bool = False,\n    filter_large_gibbs: bool = True\n) -&gt; nx.MultiDiGraph:\n    \"\"\"\n        generates a networkx.multidigraph from a .json file of reactions and quantum_dict generated with reactit and GetEnergyandVibrationsVASP\n        needs a sanity check on the file though\n\n        file takes the format of a dictionary with:\n        {\n    '&lt;compound&gt;': {\n        'atoms': ase.Atoms.asdict(),\n        'pointgroup': &lt;pointgroup&gt;,\n        'spin': &lt;spin&gt;,\n        'rotation_num': &lt;rotation_num&gt;,\n        'islinear': &lt;islinear&gt;,\n        'energy': &lt;energy&gt;,\n        'vibrations': &lt;vibrations\n        }\n    'reactions':list({\n        'reaction_string':&lt;reaction_string&gt;,\n                      'reactants':dict(reactants),\n                      'products':dict(products)}\n                      )\n        }\n\n        reactions can be generated with https://github.com/badw/reactit.git\n    \"\"\"\n    quantum_dict = loadfn(filename)\n\n    # some metadata for posterity\n    if \"metadata\" in quantum_dict:\n        self.quantum_dict_metadata = quantum_dict[\"metadata\"]\n    try:\n        self.compound_reference = {k: v[\"SMILES\"]\n                                   for k, v in quantum_dict.items() if k not in [\"metadata\", \"reactions\"]}\n    except Exception:\n        self.compound_reference = [list(quantum_dict)]\n\n    rge = ReactionGibbsandEquilibrium(quantum_dict, log_K=log_K)\n    applied_reactions = []\n    for reaction in quantum_dict[\"reactions\"].values():\n        if (\n            len(reaction[\"reactants\"]) + len(reaction[\"products\"])\n            &lt;= max_reaction_length\n        ):\n            g_k_dict = rge.get_reaction_gibbs_and_equilibrium(\n                reaction=reaction, temperature=temperature, pressure=pressure, filter_large_gibbs=filter_large_gibbs\n            )\n            g_k_dict[\"r\"] = reaction\n            if filter_large_gibbs:\n                if np.abs(g_k_dict[\"g\"]) &lt;= self.gibbs_filter:\n                    applied_reactions.append(g_k_dict)\n            else:\n                applied_reactions.append(g_k_dict)\n\n    graph = self.generate_multidigraph(\n        applied_reactions=applied_reactions, temperature=temperature, log_K=log_K\n    )\n    rge.raise_warnings()\n\n    return graph\n</code></pre>"},{"location":"api/generate/#arcs.generate.GraphGenerator.reduce_reactions","title":"reduce_reactions  <code>staticmethod</code>","text":"<pre><code>reduce_reactions(reactions_list, max_reaction_length=4)\n</code></pre> Source code in <code>src/arcs/generate.py</code> <pre><code>@staticmethod\ndef reduce_reactions(reactions_list, max_reaction_length=4):\n    rs = []\n    for reaction in reactions_list.values():\n        if (\n                len(reaction[\"reactants\"]) + len(reaction[\"products\"])\n            &lt;= max_reaction_length\n        ):\n            rs.append(reaction)\n    return rs\n</code></pre>"},{"location":"api/generate/#arcs.generate.GraphGenerator.from_dict","title":"from_dict","text":"<pre><code>from_dict(\n    quantum_dict: str,\n    temperature: float,\n    pressure: float,\n    max_reaction_length: int = 5,\n    log_K: bool = False,\n    filter_large_gibbs: bool = True,\n) -&gt; MultiDiGraph\n</code></pre> <pre><code>generates a networkx.multidigraph from a dict representation of the .json file of reactions and quantum_dict generated with reactit and GetEnergyandVibrationsVASP\nneeds a sanity check on the file though\n\ndictionary takes the format of:\n        {\n</code></pre> <p>'': {     'atoms': ase.Atoms.asdict(),     'pointgroup': ,     'spin': ,     'rotation_num': ,     'islinear': ,     'energy': ,     'vibrations': &lt;vibrations     } 'reactions':list({     'reaction_string':,                   'reactants':dict(reactants),                   'products':dict(products)}                   )     } <pre><code>reactions can be generated with https://github.com/badw/reactit.git\n</code></pre> Source code in <code>src/arcs/generate.py</code> <pre><code>def from_dict(\n    self,\n    quantum_dict: str,\n    temperature: float,  # in K\n    pressure: float,  # in bar\n    max_reaction_length: int = 5,\n    log_K: bool = False,\n    filter_large_gibbs: bool = True\n) -&gt; nx.MultiDiGraph:\n    \"\"\"\n        generates a networkx.multidigraph from a dict representation of the .json file of reactions and quantum_dict generated with reactit and GetEnergyandVibrationsVASP\n        needs a sanity check on the file though\n\n        dictionary takes the format of:\n                {\n    '&lt;compound&gt;': {\n        'atoms': ase.Atoms.asdict(),\n        'pointgroup': &lt;pointgroup&gt;,\n        'spin': &lt;spin&gt;,\n        'rotation_num': &lt;rotation_num&gt;,\n        'islinear': &lt;islinear&gt;,\n        'energy': &lt;energy&gt;,\n        'vibrations': &lt;vibrations\n        }\n    'reactions':list({\n        'reaction_string':&lt;reaction_string&gt;,\n                      'reactants':dict(reactants),\n                      'products':dict(products)}\n                      )\n        }\n\n        reactions can be generated with https://github.com/badw/reactit.git\n    \"\"\"\n\n    # some metadata for posterity\n    if \"metadata\" in quantum_dict:\n        self.quantum_dict_metadata = quantum_dict[\"metadata\"]\n\n    try:\n        self.compound_reference = {k: v[\"SMILES\"]\n                                   for k, v in quantum_dict.items() if k not in [\"metadata\", \"reactions\"]}\n    except Exception:\n        self.compound_reference = [list(quantum_dict)]\n\n    rge = ReactionGibbsandEquilibrium(quantum_dict, log_K=log_K)\n    applied_reactions = []\n    reduced_reactions = self.reduce_reactions(\n        quantum_dict[\"reactions\"], max_reaction_length=max_reaction_length)\n\n    for reaction in reduced_reactions:\n        # if (\n        #    len(reaction[\"reactants\"]) + len(reaction[\"products\"])\n        #    &lt;= max_reaction_length\n        # ):\n        g_k_dict = rge.get_reaction_gibbs_and_equilibrium(\n            reaction=reaction, temperature=temperature, pressure=pressure, filter_large_gibbs=filter_large_gibbs\n        )\n        g_k_dict[\"r\"] = reaction\n        if filter_large_gibbs:\n            if np.abs(g_k_dict[\"g\"]) &lt;= self.gibbs_filter:\n                applied_reactions.append(g_k_dict)\n        else:\n            applied_reactions.append(g_k_dict)\n\n    graph = self.generate_multidigraph(\n        applied_reactions=applied_reactions, temperature=temperature\n    )\n    return graph\n</code></pre>"},{"location":"api/generate/#thermodynamics","title":"Thermodynamics","text":""},{"location":"api/generate/#arcs.generate.ReactionGibbsandEquilibrium","title":"ReactionGibbsandEquilibrium","text":"<pre><code>ReactionGibbsandEquilibrium(\n    reaction_input: dict, log_K: bool = False\n)\n</code></pre> <p>class that takes the relevant first principles data generated by GetEnergyandVibrationsVASP and converts it into Gibbs Free Energies and equilibrium constants for a given reaction</p> Source code in <code>src/arcs/generate.py</code> <pre><code>def __init__(self, reaction_input: dict, log_K: bool = False):\n\n    self.reaction_input = reaction_input\n    self.gibbs_warnings = {}\n    self.log_K = log_K\n\n    warnings.filterwarnings(\"ignore\", category=RuntimeWarning)\n</code></pre>"},{"location":"api/generate/#arcs.generate.ReactionGibbsandEquilibrium.reaction_input","title":"reaction_input  <code>instance-attribute</code>","text":"<pre><code>reaction_input = reaction_input\n</code></pre>"},{"location":"api/generate/#arcs.generate.ReactionGibbsandEquilibrium.gibbs_warnings","title":"gibbs_warnings  <code>instance-attribute</code>","text":"<pre><code>gibbs_warnings = {}\n</code></pre>"},{"location":"api/generate/#arcs.generate.ReactionGibbsandEquilibrium.log_K","title":"log_K  <code>instance-attribute</code>","text":"<pre><code>log_K = log_K\n</code></pre>"},{"location":"api/generate/#arcs.generate.ReactionGibbsandEquilibrium.Gibbs","title":"Gibbs  <code>staticmethod</code>","text":"<pre><code>Gibbs(\n    quantum_dict: dict, temperature: float, pressure: float\n) -&gt; float\n</code></pre> <p>uses ASE's IdealGasThermo to calculate the relevant Gibbs Free Energy of a given compound at a specific temperature (K) and pressure (bar converted to Pa automatically)</p> Source code in <code>src/arcs/generate.py</code> <pre><code>@staticmethod\ndef Gibbs(\n    quantum_dict: dict,\n    temperature: float,  # in K\n    pressure: float,  # in bar\n) -&gt; float:\n    \"\"\"\n    uses ASE's IdealGasThermo to calculate the relevant Gibbs Free Energy of a given compound at a specific temperature (K) and pressure (bar converted to Pa automatically)\n    \"\"\"\n    igt = IdealGasThermo(\n        vib_energies=quantum_dict[\"vibrations\"],\n        geometry=quantum_dict[\"islinear\"],\n        potentialenergy=quantum_dict[\"energy\"],\n        atoms=Atoms.fromdict(quantum_dict[\"atoms\"]),\n        symmetrynumber=quantum_dict[\"rotation_num\"],\n        spin=quantum_dict[\"spin\"],\n        natoms=Atoms.fromdict(\n            quantum_dict[\"atoms\"]).get_global_number_of_atoms(),\n        ignore_imag_modes=True,\n    )\n\n    gibbs_free_energy = igt.get_gibbs_energy(\n        temperature, pressure * 100000, verbose=False\n    )\n\n    return gibbs_free_energy\n</code></pre>"},{"location":"api/generate/#arcs.generate.ReactionGibbsandEquilibrium.raise_warnings","title":"raise_warnings","text":"<pre><code>raise_warnings()\n</code></pre> Source code in <code>src/arcs/generate.py</code> <pre><code>def raise_warnings(self):\n    for compound, warn_info in self.gibbs_warnings.items():\n        warnings.warn(f\"{compound}: {warn_info['warning']}\", UserWarning)\n</code></pre>"},{"location":"api/generate/#arcs.generate.ReactionGibbsandEquilibrium.reaction_gibbs","title":"reaction_gibbs","text":"<pre><code>reaction_gibbs(\n    reaction: dict, pressure: float, temperature: float\n) -&gt; float\n</code></pre> <p>returns the Gibbs Free Energy of Reaction for a given reaction (dict). example reaction: {'reaction_string': '1 H2O + 1 H2CO = 1 H2 + 1 CH2O2', 'reactants': {'H2O': 1, 'H2CO': 1}, 'products': {'H2': 1, 'CH2O2': 1}},</p> Source code in <code>src/arcs/generate.py</code> <pre><code>def reaction_gibbs(\n    self,\n    reaction: dict,  # reactit dict\n    pressure: float,  # in bar\n    temperature: float,  # \u00a0in K\n) -&gt; float:\n    \"\"\"\n    returns the Gibbs Free Energy of Reaction for a given reaction (dict).\n    example reaction:\n    {'reaction_string': '1 H2O + 1 H2CO = 1 H2 + 1 CH2O2',\n    'reactants': {'H2O': 1, 'H2CO': 1},\n    'products': {'H2': 1, 'CH2O2': 1}},\n    \"\"\"\n    products = reaction[\"products\"]\n    reactants = reaction[\"reactants\"]\n    num_atoms = 0\n    for reactant, coeff in reactants.items():\n        num_atoms += np.sum(list(parse_molecule(reactant).values())) * coeff\n    reaction_compounds = list(products) + list(reactants)\n\n    gibbs = {}\n    for compound in reaction_compounds:\n        with warnings.catch_warnings(record=True) as w:\n            # warnings.simplefilter(\"always\")\n            gfe = self.Gibbs(\n                quantum_dict=self.reaction_input[compound],\n                temperature=temperature,\n                pressure=pressure,\n            )\n            gibbs[compound] = gfe\n            if w:\n                self.gibbs_warnings[compound] = {\n                    \"warning\": str(w[0].message)}\n\n    prod_sum = np.sum(\n        [\n            gibbs[compound] * products[compound]\n            for compound in gibbs\n            if compound in products\n        ]\n    )\n    reac_sum = np.sum(\n        [\n            gibbs[compound] * reactants[compound]\n            for compound in gibbs\n            if compound in reactants\n        ]\n    )\n\n    return float(prod_sum - reac_sum)\n</code></pre>"},{"location":"api/generate/#arcs.generate.ReactionGibbsandEquilibrium.equilibrium_constant","title":"equilibrium_constant  <code>staticmethod</code>","text":"<pre><code>equilibrium_constant(\n    gibbs_free_energy: float,\n    temperature: float,\n    log_K: bool = False,\n) -&gt; float\n</code></pre> <p>returns an equilibrium constant, K, from the Gibbs Free Energy of Reaction (in eV) from DeltaG = -k_B T ln(K)</p> Source code in <code>src/arcs/generate.py</code> <pre><code>@staticmethod\ndef equilibrium_constant(\n    gibbs_free_energy: float,\n    temperature: float,  # in K\n    log_K: bool = False\n) -&gt; float:\n    \"\"\"\n    returns an equilibrium constant, K, from the Gibbs Free Energy of Reaction (in eV)\n    from DeltaG = -k_B T ln(K)\n    \"\"\"\n    if log_K:\n        K = -(gibbs_free_energy) / ((Boltzmann / e) * temperature)\n    else:\n        K = np.exp(-(gibbs_free_energy) / ((Boltzmann / e) * temperature))\n\n    return K\n</code></pre>"},{"location":"api/generate/#arcs.generate.ReactionGibbsandEquilibrium.get_reaction_gibbs_and_equilibrium","title":"get_reaction_gibbs_and_equilibrium","text":"<pre><code>get_reaction_gibbs_and_equilibrium(\n    reaction,\n    temperature: float,\n    pressure: float,\n    filter_large_gibbs: bool = True,\n) -&gt; dict\n</code></pre> <p>given a reaction dictionary object (from reactit), the function generates a dictionary with both gibbs free energy and equilibrium constant</p> <p>example reaction dictionary:</p> <p>{'reaction_string': '1 H2O + 1 H2CO = 1 H2 + 1 CH2O2', 'reactants': {'H2O': 1, 'H2CO': 1}, 'products': {'H2': 1, 'CH2O2': 1}},</p> Source code in <code>src/arcs/generate.py</code> <pre><code>def get_reaction_gibbs_and_equilibrium(\n    self,\n    reaction,\n    temperature: float,  # in K\n    pressure: float,  # in bar\n    filter_large_gibbs: bool = True\n) -&gt; dict:\n    \"\"\"\n    given a reaction dictionary object (from reactit), the function generates a dictionary with both gibbs free energy and equilibrium constant\n\n    example reaction dictionary:\n\n    {'reaction_string': '1 H2O + 1 H2CO = 1 H2 + 1 CH2O2',\n    'reactants': {'H2O': 1, 'H2CO': 1},\n    'products': {'H2': 1, 'CH2O2': 1}},\n\n    \"\"\"\n    if isinstance(reaction, str):\n        from reactit import ReactionGenerator\n\n        try:\n            reaction_dict = ReactionGenerator.get_reactants_products(\n                reaction)\n            reaction = {\n                \"reaction_string\": reaction,\n                \"reactants\": reaction_dict[0],\n                \"products\": reaction_dict[1],\n            }\n        except Exception as e:\n            print(e)\n\n    gibbs_free_energy = self.reaction_gibbs(\n        reaction=reaction, temperature=temperature, pressure=pressure\n    )\n    equilibrium_constant = self.equilibrium_constant(\n        gibbs_free_energy=gibbs_free_energy, temperature=temperature, log_K=self.log_K\n    )\n    if not filter_large_gibbs:\n        if equilibrium_constant == np.inf:\n            warnings.warn(\n                f\"{reaction['reaction_string']} has K={equilibrium_constant}\")\n        elif equilibrium_constant == float(0):\n            warnings.warn(\n                f\"{reaction['reaction_string']} has K={equilibrium_constant}\")\n\n    return {\"g\": gibbs_free_energy, \"log_k\": equilibrium_constant} if self.log_K else {\"g\": gibbs_free_energy, \"k\": equilibrium_constant}\n</code></pre>"},{"location":"api/generate/#initial-concentrations","title":"Initial concentrations","text":""},{"location":"api/generate/#arcs.generate.GenerateInitialConcentrations","title":"GenerateInitialConcentrations","text":"<pre><code>GenerateInitialConcentrations(graph: MultiDiGraph)\n</code></pre> <p>a class for generating initial concentrations in a standardised way typically in ppm or 1e-6</p> <p>this should be used to update concentrations and log them as well in future</p> Source code in <code>src/arcs/generate.py</code> <pre><code>def __init__(self, graph: nx.MultiDiGraph):\n    self.graph = graph\n</code></pre>"},{"location":"api/generate/#arcs.generate.GenerateInitialConcentrations.graph","title":"graph  <code>instance-attribute</code>","text":"<pre><code>graph = graph\n</code></pre>"},{"location":"api/generate/#arcs.generate.GenerateInitialConcentrations.all_random","title":"all_random","text":"<pre><code>all_random(include_co2=True) -&gt; dict\n</code></pre> <p>generate fully random concentrations returns a dictionary of compounds with a random concentration</p> Source code in <code>src/arcs/generate.py</code> <pre><code>def all_random(self, include_co2=True) -&gt; dict:\n    \"\"\"\n    generate fully random concentrations\n    returns a dictionary of compounds with a random concentration\n    \"\"\"\n    compounds = [node for node in self.graph.nodes()\n                 if isinstance(node, str)]\n    ic = {c: np.random.random() / 1e5 for c in compounds}\n    if not include_co2:\n        ic[\"CO2\"] = 1\n    self.ic = ic\n    return self.ic\n</code></pre>"},{"location":"api/generate/#arcs.generate.GenerateInitialConcentrations.all_zero","title":"all_zero","text":"<pre><code>all_zero(include_co2=True) -&gt; dict\n</code></pre> <p>makes a dictionary of blank concentrations returns a dictionary of compounds with 0 concentration</p> Source code in <code>src/arcs/generate.py</code> <pre><code>def all_zero(self, include_co2=True) -&gt; dict:\n    \"\"\"\n    makes a dictionary of blank concentrations\n    returns a dictionary of compounds with 0 concentration\n    \"\"\"\n    compounds = [node for node in self.graph.nodes()\n                 if isinstance(node, str)]\n    ic = {c: 0 for c in compounds}\n    if not include_co2:\n        ic[\"CO2\"] = 1\n    self.ic = ic\n    return self.ic\n</code></pre>"},{"location":"api/generate/#arcs.generate.GenerateInitialConcentrations.specific_random","title":"specific_random","text":"<pre><code>specific_random(compounds=list, include_co2=True) -&gt; dict\n</code></pre> <p>makes a dictionary of compounds with only certain compounds given random concentrations i.e. specific_random(compounds=['CO2','H2O','O2'])</p> Source code in <code>src/arcs/generate.py</code> <pre><code>def specific_random(self, compounds=list, include_co2=True) -&gt; dict:\n    \"\"\"\n    makes a dictionary of compounds with only certain compounds given random concentrations\n    i.e.\n    specific_random(compounds=['CO2','H2O','O2'])\n    \"\"\"\n    full_list = [n for n in self.graph.nodes() if isinstance(n, str)]\n    ic = {}\n    for c in full_list:\n        if c in compounds:\n            ic[c] = np.random.random() / 1e6\n        else:\n            ic[c] = 0\n    if not include_co2:\n        ic[\"CO2\"] = 1\n    self.ic = ic\n    return self.ic\n</code></pre>"},{"location":"api/generate/#arcs.generate.GenerateInitialConcentrations.update_ic","title":"update_ic","text":"<pre><code>update_ic(update_dict, include_co2=True) -&gt; dict\n</code></pre> <p>update dict = {'CO2':1e-6,'H2O':300e-5} etc. used to update dictionary items need to have this log previous concentrations as well.</p> Source code in <code>src/arcs/generate.py</code> <pre><code>def update_ic(self, update_dict, include_co2=True) -&gt; dict:\n    \"\"\"\n    update dict = {'CO2':1e-6,'H2O':300e-5} etc.\n    used to update dictionary items\n    need to have this log previous concentrations as well.\n    \"\"\"\n    try:\n        hasattr(self.ic, \"ic\")\n    except Exception:\n        # if not initial concentrations created previously\n        self.all_zero(include_co2=include_co2)\n    for k, v in update_dict.items():\n        self.ic[k] = v\n    return self.ic\n</code></pre>"},{"location":"api/generate/#quantum-data-helpers","title":"Quantum-data helpers","text":"<p>These classes convert electronic-structure output into the per-compound dictionaries ARCS consumes.</p>"},{"location":"api/generate/#arcs.generate.GetEnergyandVibrationsVASP","title":"GetEnergyandVibrationsVASP","text":"<pre><code>GetEnergyandVibrationsVASP(\n    relax_directory, vibrations_directory\n)\n</code></pre> <p>Class to get the Total Energy and Vibrations from a directory containing a calculations</p> Source code in <code>src/arcs/generate.py</code> <pre><code>def __init__(self, relax_directory, vibrations_directory):\n    self.relax = relax_directory\n    self.vibrations = vibrations_directory\n</code></pre>"},{"location":"api/generate/#arcs.generate.GetEnergyandVibrationsVASP.relax","title":"relax  <code>instance-attribute</code>","text":"<pre><code>relax = relax_directory\n</code></pre>"},{"location":"api/generate/#arcs.generate.GetEnergyandVibrationsVASP.vibrations","title":"vibrations  <code>instance-attribute</code>","text":"<pre><code>vibrations = vibrations_directory\n</code></pre>"},{"location":"api/generate/#arcs.generate.GetEnergyandVibrationsVASP.get_initial_magnetic_moments","title":"get_initial_magnetic_moments  <code>staticmethod</code>","text":"<pre><code>get_initial_magnetic_moments(aseatoms: Atoms) -&gt; list\n</code></pre> Source code in <code>src/arcs/generate.py</code> <pre><code>@staticmethod\ndef get_initial_magnetic_moments(aseatoms: ase.Atoms) -&gt; list:\n    magmoms = []\n    for atomic_number in aseatoms.get_atomic_numbers():\n        magmoms.append([0 if atomic_number % 2 == 0 else 1][0])\n    return magmoms\n</code></pre>"},{"location":"api/generate/#arcs.generate.GetEnergyandVibrationsVASP.get_atoms","title":"get_atoms","text":"<pre><code>get_atoms() -&gt; Atoms\n</code></pre> <p>generates an ASE.Atoms object from a given POSCAR with magnetic moments</p> Source code in <code>src/arcs/generate.py</code> <pre><code>def get_atoms(self) -&gt; ase.Atoms:\n    \"\"\"\n    generates an ASE.Atoms object from a given POSCAR with magnetic moments\n    \"\"\"\n\n    aseatoms = read(\"{}/POSCAR\".format(self.relax))\n\n    aseatoms.set_initial_magnetic_moments(\n        self.get_initial_magnetic_moments(aseatoms=aseatoms)\n    )\n\n    return aseatoms\n</code></pre>"},{"location":"api/generate/#arcs.generate.GetEnergyandVibrationsVASP.get_energy","title":"get_energy","text":"<pre><code>get_energy() -&gt; float\n</code></pre> <p>grabs the total energy from a VASP OUTCAR file (assumes one formula unit per cell)</p> Source code in <code>src/arcs/generate.py</code> <pre><code>def get_energy(self) -&gt; float:\n    \"\"\"\n    grabs the total energy from a VASP OUTCAR file (assumes one formula unit per cell)\n    \"\"\"\n    outcar = open(\"{}/OUTCAR\".format(self.relax), \"r\").readlines()\n\n    for line in outcar:\n        if \"y=\" in line:\n            energy = float(line.split()[-1])\n    if len(list(dict.fromkeys(self.get_atoms().get_atomic_numbers()))) == 1:\n        if not any(\n            x in self.get_atoms().symbols.get_chemical_formula()\n            for x in [\"H2\", \"O2\", \"N2\", \"S8\"]  # elemental species...\n        ):\n            energy = energy / self.atoms().get_global_number_of_atoms()\n\n    return energy\n</code></pre>"},{"location":"api/generate/#arcs.generate.GetEnergyandVibrationsVASP.get_spin","title":"get_spin","text":"<pre><code>get_spin() -&gt; int\n</code></pre> <p>determines the spin of a system</p> Source code in <code>src/arcs/generate.py</code> <pre><code>def get_spin(self) -&gt; int:\n    \"\"\"\n    determines the spin of a system\n    \"\"\"\n    if self.get_atoms().get_chemical_formula() in [\"O2\", \"CO3\"]:\n        return 1\n    else:\n        outcar = open(\"{}/OUTCAR\".format(self.relax), \"r\").readlines()\n        for line in outcar:\n            if \"NELECT\" in line:\n                nelect = float(line.split()[2])\n                return [0 if nelect % 2 == 0 else 0.5][0]\n</code></pre>"},{"location":"api/generate/#arcs.generate.GetEnergyandVibrationsVASP.get_pointgroup","title":"get_pointgroup","text":"<pre><code>get_pointgroup() -&gt; str\n</code></pre> <p>uses pymatgen's pymatgen.symmetry.analyzer.PointGroupAnalyzer class to determine the point group of an ase.Atoms object returns a string</p> Source code in <code>src/arcs/generate.py</code> <pre><code>def get_pointgroup(self) -&gt; str:\n    \"\"\"\n    uses pymatgen's pymatgen.symmetry.analyzer.PointGroupAnalyzer class to determine the point group of an ase.Atoms object\n    returns a string\n    \"\"\"\n    aseatoms = self.get_atoms()\n    pg = PointGroupAnalyzer(\n        AseAtomsAdaptor.get_molecule(aseatoms)).get_pointgroup()\n\n    return pg.sch_symbol\n</code></pre>"},{"location":"api/generate/#arcs.generate.GetEnergyandVibrationsVASP.islinear","title":"islinear","text":"<pre><code>islinear() -&gt; str\n</code></pre> <p>determines whether the molecule is linear - can determine this from the point group (if * the molecule is linear)</p> Source code in <code>src/arcs/generate.py</code> <pre><code>def islinear(self) -&gt; str:\n    \"\"\"\n    determines whether the molecule is linear - can determine this from the point group (if * the molecule is linear)\n    \"\"\"\n    num_at = self.get_atoms()\n    if num_at.get_global_number_of_atoms() == 1:\n        return \"monatomic\"\n    else:\n        pg = self.get_pointgroup()\n        if \"*\" in pg:\n            return \"linear\"\n        else:\n            return \"nonlinear\"\n</code></pre>"},{"location":"api/generate/#arcs.generate.GetEnergyandVibrationsVASP.get_rotation_num","title":"get_rotation_num","text":"<pre><code>get_rotation_num() -&gt; int\n</code></pre> <p>determines the rotational number from the point group returns an integer</p> Source code in <code>src/arcs/generate.py</code> <pre><code>def get_rotation_num(self) -&gt; int:\n    \"\"\"\n    determines the rotational number from the point group\n    returns an integer\n    \"\"\"\n    pg = [x for x in self.get_pointgroup()]\n    if pg[0] == \"C\":\n        if pg[-1] == \"i\":\n            rot = 1\n        elif pg[-1] == \"s\":\n            rot = 1\n        elif pg[-1] == \"h\":\n            rot = int(pg[1])\n        elif pg[-1] == \"v\":\n            if pg[1] == \"*\":\n                rot = 1\n            else:\n                rot = int(pg[1])\n        elif len(pg) == 2:\n            rot = int(pg[-1])\n\n    elif pg[0] == \"D\":\n        if pg[-1] == \"h\":\n            if pg[1] == \"*\":\n                rot = 2\n            else:\n                rot = 2 * int(pg[1])\n        elif pg[-1] == \"d\":\n            rot = 2 * int(pg[1])\n        elif len(pg) == 2:\n            rot = 2 * int(pg[1])\n\n    elif pg[0] == \"T\":\n        rot = 12\n\n    elif pg[0] == \"O\":\n        rot = 24\n\n    elif pg[0] == \"I\":\n        rot = 60\n\n    return rot\n</code></pre>"},{"location":"api/generate/#arcs.generate.GetEnergyandVibrationsVASP.get_vibrations","title":"get_vibrations","text":"<pre><code>get_vibrations() -&gt; list\n</code></pre> <p>gets the vibrational frequencies from a DFPT calculation as outputted in a VASP OUTCAR returns as list of both imaginary and normal modes of vibration in eV</p> Source code in <code>src/arcs/generate.py</code> <pre><code>def get_vibrations(self) -&gt; list:\n    \"\"\"\n    gets the vibrational frequencies from a DFPT calculation as outputted in a VASP OUTCAR\n    returns as list of both imaginary and normal modes of vibration in eV\n    \"\"\"\n    outcar = open(\"{}/OUTCAR\".format(self.vibrations), \"r\").readlines()\n    frequencies = []\n    # i_frequencies = []\n    for line in outcar:\n        if \"THz\" in line:\n            if \"f/i\" not in line:\n                frequencies.append(float(line.split()[-2]) / 1000)\n\n            else:  # imaginary frequencies are returned separately as a real number\n                frequencies.append(\n                    -float(line.split()[-2]) / 1000\n                )  # need to go back over this\n    return frequencies\n</code></pre>"},{"location":"api/generate/#arcs.generate.GetEnergyandVibrationsVASP.as_dict","title":"as_dict","text":"<pre><code>as_dict()\n</code></pre> Source code in <code>src/arcs/generate.py</code> <pre><code>def as_dict(self):\n    return {\n        \"atoms\": self.get_atoms().todict(),\n        \"pointgroup\": self.get_pointgroup(),\n        \"spin\": self.get_spin(),\n        \"rotation_num\": self.get_rotation_num(),\n        \"islinear\": self.islinear(),\n        \"energy\": self.get_energy(),\n        \"vibrations\": self.get_vibrations(),\n    }\n</code></pre>"},{"location":"api/generate/#arcs.generate.GetEnergyandVibrationsPsi4","title":"GetEnergyandVibrationsPsi4","text":"<pre><code>GetEnergyandVibrationsPsi4(\n    aseatoms: Atoms, energy: float, vibrations: ndarray\n)\n</code></pre> <p>Class to get the Total Energy and Vibrations from a directory containing a calculations</p> Source code in <code>src/arcs/generate.py</code> <pre><code>def __init__(self, aseatoms: ase.Atoms, energy: float, vibrations: np.ndarray):\n\n    self.inversecmtoev = 1.23981e-4\n    self.aseatoms = aseatoms\n    self.vibrations = (\n        vibrations * self.inversecmtoev\n    )  # in cm-1 (direct from psi4 output )\n    self.energy = energy * Hartree  # in Hartree?\n    self.get_atoms()\n</code></pre>"},{"location":"api/generate/#arcs.generate.GetEnergyandVibrationsPsi4.inversecmtoev","title":"inversecmtoev  <code>instance-attribute</code>","text":"<pre><code>inversecmtoev = 0.000123981\n</code></pre>"},{"location":"api/generate/#arcs.generate.GetEnergyandVibrationsPsi4.aseatoms","title":"aseatoms  <code>instance-attribute</code>","text":"<pre><code>aseatoms = aseatoms\n</code></pre>"},{"location":"api/generate/#arcs.generate.GetEnergyandVibrationsPsi4.vibrations","title":"vibrations  <code>instance-attribute</code>","text":"<pre><code>vibrations = vibrations * self.inversecmtoev\n</code></pre>"},{"location":"api/generate/#arcs.generate.GetEnergyandVibrationsPsi4.energy","title":"energy  <code>instance-attribute</code>","text":"<pre><code>energy = energy * Hartree\n</code></pre>"},{"location":"api/generate/#arcs.generate.GetEnergyandVibrationsPsi4.get_initial_magnetic_moments","title":"get_initial_magnetic_moments  <code>staticmethod</code>","text":"<pre><code>get_initial_magnetic_moments(aseatoms: Atoms) -&gt; list\n</code></pre> Source code in <code>src/arcs/generate.py</code> <pre><code>@staticmethod\ndef get_initial_magnetic_moments(aseatoms: ase.Atoms) -&gt; list:\n    magmoms = []\n    for atomic_number in aseatoms.get_atomic_numbers():\n        magmoms.append([0 if atomic_number % 2 == 0 else 1][0])\n    return magmoms\n</code></pre>"},{"location":"api/generate/#arcs.generate.GetEnergyandVibrationsPsi4.get_atoms","title":"get_atoms","text":"<pre><code>get_atoms()\n</code></pre> <p>generates an ASE.Atoms object from a given POSCAR with magnetic moments</p> Source code in <code>src/arcs/generate.py</code> <pre><code>def get_atoms(self):\n    \"\"\"\n    generates an ASE.Atoms object from a given POSCAR with magnetic moments\n    \"\"\"\n\n    self.aseatoms.set_initial_magnetic_moments(\n        self.get_initial_magnetic_moments(aseatoms=self.aseatoms)\n    )\n</code></pre>"},{"location":"api/generate/#arcs.generate.GetEnergyandVibrationsPsi4.get_energy","title":"get_energy","text":"<pre><code>get_energy() -&gt; float\n</code></pre> <p>grabs the total energy from a VASP OUTCAR file (assumes one formula unit per cell)</p> Source code in <code>src/arcs/generate.py</code> <pre><code>def get_energy(self) -&gt; float:\n    \"\"\"\n    grabs the total energy from a VASP OUTCAR file (assumes one formula unit per cell)\n    \"\"\"\n    return self.energy\n</code></pre>"},{"location":"api/generate/#arcs.generate.GetEnergyandVibrationsPsi4.get_spin","title":"get_spin","text":"<pre><code>get_spin() -&gt; int\n</code></pre> <p>determines the spin of a system</p> Source code in <code>src/arcs/generate.py</code> <pre><code>def get_spin(self) -&gt; int:\n    \"\"\"\n    determines the spin of a system\n    \"\"\"\n    if self.aseatoms.get_chemical_formula() in [\"O2\", \"CO3\"]:\n        return 1\n    else:\n        nelect = self.aseatoms.get_atomic_numbers().sum()\n        return [0 if nelect % 2 == 0 else 0.5][0]\n</code></pre>"},{"location":"api/generate/#arcs.generate.GetEnergyandVibrationsPsi4.get_pointgroup","title":"get_pointgroup","text":"<pre><code>get_pointgroup() -&gt; str\n</code></pre> <p>uses pymatgen's pymatgen.symmetry.analyzer.PointGroupAnalyzer class to determine the point group of an ase.Atoms object returns a string</p> Source code in <code>src/arcs/generate.py</code> <pre><code>def get_pointgroup(self) -&gt; str:\n    \"\"\"\n    uses pymatgen's pymatgen.symmetry.analyzer.PointGroupAnalyzer class to determine the point group of an ase.Atoms object\n    returns a string\n    \"\"\"\n    pg = PointGroupAnalyzer(\n        AseAtomsAdaptor.get_molecule(self.aseatoms)\n    ).get_pointgroup()\n\n    return pg.sch_symbol\n</code></pre>"},{"location":"api/generate/#arcs.generate.GetEnergyandVibrationsPsi4.islinear","title":"islinear","text":"<pre><code>islinear() -&gt; str\n</code></pre> <p>determines whether the molecule is linear - can determine this from the point group (if * the molecule is linear)</p> Source code in <code>src/arcs/generate.py</code> <pre><code>def islinear(self) -&gt; str:\n    \"\"\"\n    determines whether the molecule is linear - can determine this from the point group (if * the molecule is linear)\n    \"\"\"\n    if self.aseatoms.get_global_number_of_atoms() == 1:\n        return \"monatomic\"\n    else:\n        pg = self.get_pointgroup()\n        if \"*\" in pg:\n            return \"linear\"\n        else:\n            return \"nonlinear\"\n</code></pre>"},{"location":"api/generate/#arcs.generate.GetEnergyandVibrationsPsi4.get_rotation_num","title":"get_rotation_num","text":"<pre><code>get_rotation_num() -&gt; int\n</code></pre> <p>determines the rotational number from the point group returns an integer</p> Source code in <code>src/arcs/generate.py</code> <pre><code>def get_rotation_num(self) -&gt; int:\n    \"\"\"\n    determines the rotational number from the point group\n    returns an integer\n    \"\"\"\n    pg = [x for x in self.get_pointgroup()]\n    if pg[0] == \"C\":\n        if pg[-1] == \"i\":\n            rot = 1\n        elif pg[-1] == \"s\":\n            rot = 1\n        elif pg[-1] == \"h\":\n            rot = int(pg[1])\n        elif pg[-1] == \"v\":\n            if pg[1] == \"*\":\n                rot = 1\n            else:\n                rot = int(pg[1])\n        elif len(pg) == 2:\n            rot = int(pg[-1])\n\n    elif pg[0] == \"D\":\n        if pg[-1] == \"h\":\n            if pg[1] == \"*\":\n                rot = 2\n            else:\n                rot = 2 * int(pg[1])\n        elif pg[-1] == \"d\":\n            rot = 2 * int(pg[1])\n        elif len(pg) == 2:\n            rot = 2 * int(pg[1])\n\n    elif pg[0] == \"T\":\n        rot = 12\n\n    elif pg[0] == \"O\":\n        rot = 24\n\n    elif pg[0] == \"I\":\n        rot = 60\n\n    return rot\n</code></pre>"},{"location":"api/generate/#arcs.generate.GetEnergyandVibrationsPsi4.as_dict","title":"as_dict","text":"<pre><code>as_dict()\n</code></pre> Source code in <code>src/arcs/generate.py</code> <pre><code>def as_dict(self):\n    return {\n        \"atoms\": self.aseatoms.todict(),\n        \"pointgroup\": self.get_pointgroup(),\n        \"spin\": self.get_spin(),\n        \"rotation_num\": self.get_rotation_num(),\n        \"islinear\": self.islinear(),\n        \"energy\": self.get_energy(),\n        \"vibrations\": self.vibrations,\n    }\n</code></pre>"},{"location":"api/generate/#arcs.generate.GetEnergyandVibrationsAseCalc","title":"GetEnergyandVibrationsAseCalc","text":"<pre><code>GetEnergyandVibrationsAseCalc(\n    aseatomscalc: Atoms, asevibrationscalc\n)\n</code></pre> <p>Class to get the Total Energy and Vibrations from a directory containing a calculations</p> Source code in <code>src/arcs/generate.py</code> <pre><code>def __init__(self, aseatomscalc: ase.Atoms, asevibrationscalc):\n    self.aseatomscalc = aseatomscalc\n    self.asevibrationscalc = asevibrationscalc\n\n    self.get_atoms()\n</code></pre>"},{"location":"api/generate/#arcs.generate.GetEnergyandVibrationsAseCalc.aseatomscalc","title":"aseatomscalc  <code>instance-attribute</code>","text":"<pre><code>aseatomscalc = aseatomscalc\n</code></pre>"},{"location":"api/generate/#arcs.generate.GetEnergyandVibrationsAseCalc.asevibrationscalc","title":"asevibrationscalc  <code>instance-attribute</code>","text":"<pre><code>asevibrationscalc = asevibrationscalc\n</code></pre>"},{"location":"api/generate/#arcs.generate.GetEnergyandVibrationsAseCalc.get_initial_magnetic_moments","title":"get_initial_magnetic_moments  <code>staticmethod</code>","text":"<pre><code>get_initial_magnetic_moments(aseatoms: Atoms) -&gt; list\n</code></pre> Source code in <code>src/arcs/generate.py</code> <pre><code>@staticmethod\ndef get_initial_magnetic_moments(aseatoms: Atoms) -&gt; list:\n    magmoms = []\n    for atomic_number in aseatoms.get_atomic_numbers():\n        magmoms.append([0 if atomic_number % 2 == 0 else 1][0])\n    return magmoms\n</code></pre>"},{"location":"api/generate/#arcs.generate.GetEnergyandVibrationsAseCalc.get_atoms","title":"get_atoms","text":"<pre><code>get_atoms()\n</code></pre> <p>generates an ASE.Atoms object from a given POSCAR with magnetic moments</p> Source code in <code>src/arcs/generate.py</code> <pre><code>def get_atoms(self):\n    \"\"\"\n    generates an ASE.Atoms object from a given POSCAR with magnetic moments\n    \"\"\"\n\n    self.aseatomscalc.set_initial_magnetic_moments(\n        self.get_initial_magnetic_moments(aseatoms=self.aseatomscalc)\n    )\n</code></pre>"},{"location":"api/generate/#arcs.generate.GetEnergyandVibrationsAseCalc.get_energy","title":"get_energy","text":"<pre><code>get_energy() -&gt; float\n</code></pre> <p>grabs the total energy from a VASP OUTCAR file (assumes one formula unit per cell)</p> Source code in <code>src/arcs/generate.py</code> <pre><code>def get_energy(self) -&gt; float:\n    \"\"\"\n    grabs the total energy from a VASP OUTCAR file (assumes one formula unit per cell)\n    \"\"\"\n    return self.aseatomscalc.get_potential_energy()\n</code></pre>"},{"location":"api/generate/#arcs.generate.GetEnergyandVibrationsAseCalc.get_spin","title":"get_spin","text":"<pre><code>get_spin() -&gt; int\n</code></pre> <p>determines the spin of a system</p> Source code in <code>src/arcs/generate.py</code> <pre><code>def get_spin(self) -&gt; int:\n    \"\"\"\n    determines the spin of a system\n    \"\"\"\n    if self.aseatomscalc.get_chemical_formula() in [\"O2\", \"CO3\"]:\n        return 1\n    else:\n        nelect = self.aseatomscalc.get_atomic_numbers().sum()\n        return [0 if nelect % 2 == 0 else 0.5][0]\n</code></pre>"},{"location":"api/generate/#arcs.generate.GetEnergyandVibrationsAseCalc.get_pointgroup","title":"get_pointgroup","text":"<pre><code>get_pointgroup() -&gt; str\n</code></pre> <p>uses pymatgen's pymatgen.symmetry.analyzer.PointGroupAnalyzer class to determine the point group of an ase.Atoms object returns a string</p> Source code in <code>src/arcs/generate.py</code> <pre><code>def get_pointgroup(self) -&gt; str:\n    \"\"\"\n    uses pymatgen's pymatgen.symmetry.analyzer.PointGroupAnalyzer class to determine the point group of an ase.Atoms object\n    returns a string\n    \"\"\"\n    pg = PointGroupAnalyzer(\n        AseAtomsAdaptor.get_molecule(self.aseatomscalc)\n    ).get_pointgroup()\n\n    return pg.sch_symbol\n</code></pre>"},{"location":"api/generate/#arcs.generate.GetEnergyandVibrationsAseCalc.islinear","title":"islinear","text":"<pre><code>islinear() -&gt; str\n</code></pre> <p>determines whether the molecule is linear - can determine this from the point group (if * the molecule is linear)</p> Source code in <code>src/arcs/generate.py</code> <pre><code>def islinear(self) -&gt; str:\n    \"\"\"\n    determines whether the molecule is linear - can determine this from the point group (if * the molecule is linear)\n    \"\"\"\n    if self.aseatomscalc.get_global_number_of_atoms() == 1:\n        return \"monatomic\"\n    else:\n        pg = self.get_pointgroup()\n        if \"*\" in pg:\n            return \"linear\"\n        else:\n            return \"nonlinear\"\n</code></pre>"},{"location":"api/generate/#arcs.generate.GetEnergyandVibrationsAseCalc.get_rotation_num","title":"get_rotation_num","text":"<pre><code>get_rotation_num() -&gt; int\n</code></pre> <p>determines the rotational number from the point group returns an integer</p> Source code in <code>src/arcs/generate.py</code> <pre><code>def get_rotation_num(self) -&gt; int:\n    \"\"\"\n    determines the rotational number from the point group\n    returns an integer\n    \"\"\"\n    pg = [x for x in self.get_pointgroup()]\n    if pg[0] == \"C\":\n        if pg[-1] == \"i\":\n            rot = 1\n        elif pg[-1] == \"s\":\n            rot = 1\n        elif pg[-1] == \"h\":\n            rot = int(pg[1])\n        elif pg[-1] == \"v\":\n            if pg[1] == \"*\":\n                rot = 1\n            else:\n                rot = int(pg[1])\n        elif len(pg) == 2:\n            rot = int(pg[-1])\n\n    elif pg[0] == \"D\":\n        if pg[-1] == \"h\":\n            if pg[1] == \"*\":\n                rot = 2\n            else:\n                rot = 2 * int(pg[1])\n        elif pg[-1] == \"d\":\n            rot = 2 * int(pg[1])\n        elif len(pg) == 2:\n            rot = 2 * int(pg[1])\n\n    elif pg[0] == \"T\":\n        rot = 12\n\n    elif pg[0] == \"O\":\n        rot = 24\n\n    elif pg[0] == \"I\":\n        rot = 60\n\n    return rot\n</code></pre>"},{"location":"api/generate/#arcs.generate.GetEnergyandVibrationsAseCalc.get_vibrations","title":"get_vibrations","text":"<pre><code>get_vibrations()\n</code></pre> <p>returns the frequencies and eigenvectors of the vibrations</p> Source code in <code>src/arcs/generate.py</code> <pre><code>def get_vibrations(self):\n    \"\"\"\n    returns the frequencies and eigenvectors of the vibrations\n    \"\"\"\n    if isinstance(self.asevibrationscalc, np.ndarray):\n        return self.asevibrationscalc\n    else:\n        return self.asevibrationscalc.get_energies()\n</code></pre>"},{"location":"api/generate/#arcs.generate.GetEnergyandVibrationsAseCalc.as_dict","title":"as_dict","text":"<pre><code>as_dict()\n</code></pre> Source code in <code>src/arcs/generate.py</code> <pre><code>def as_dict(self):\n    return {\n        \"atoms\": self.aseatomscalc.todict(),\n        \"pointgroup\": self.get_pointgroup(),\n        \"spin\": self.get_spin(),\n        \"rotation_num\": self.get_rotation_num(),\n        \"islinear\": self.islinear(),\n        \"energy\": self.get_energy(),\n        \"vibrations\": self.get_vibrations(),\n    }\n</code></pre>"},{"location":"api/generate/#module-functions","title":"Module functions","text":""},{"location":"api/generate/#arcs.generate.parse_molecule","title":"parse_molecule","text":"<pre><code>parse_molecule(formula: str) -&gt; dict\n</code></pre> <p>parses a molecule string i.e. 'H2O' into a dictionary broken down into elemental counts i.e. {'H':2,'O':1} for more complex compounds like (CH3)2CHCH2NH2 this should be represented as \"CH3_2_CHCH2NH2\" taken with permission from https://github.com/badw/reactit.git</p> Source code in <code>src/arcs/generate.py</code> <pre><code>def parse_molecule(formula: str) -&gt; dict:\n    \"\"\"\n    parses a molecule string i.e. 'H2O' into a dictionary broken down into elemental counts\n    i.e. {'H':2,'O':1}\n    for more complex compounds like (CH3)2CHCH2NH2\n    this should be represented as \"CH3_2_CHCH2NH2\"\n    taken with permission from https://github.com/badw/reactit.git\n    \"\"\"\n    parts = [p for p in re.split(r'_(\\d+)_?', formula) if p]\n    total_counts = defaultdict(int)\n    last_group_counts = defaultdict(int)\n\n    i = 0\n    while i &lt; len(parts):\n        token = parts[i]\n\n        if token.isdigit():\n            multiplier = int(token)\n            for elem, count in last_group_counts.items():\n                total_counts[elem] += count * (multiplier - 1)\n            last_group_counts = defaultdict(int)\n        else:\n            current_group_counts = defaultdict(int)\n            elements = re.findall(r\"([A-Z][a-z]?)(\\d*)\", token)\n\n            for elem, count in elements:\n                c = int(count) if count else 1\n                total_counts[elem] += c\n                current_group_counts[elem] += c\n\n            last_group_counts = current_group_counts\n\n        i += 1\n\n    return dict(total_counts)\n</code></pre>"},{"location":"api/generate/#arcs.generate.get_compound_directory","title":"get_compound_directory","text":"<pre><code>get_compound_directory(base, compound, size)\n</code></pre> Source code in <code>src/arcs/generate.py</code> <pre><code>def get_compound_directory(base, compound, size):\n    return os.path.join(base, compound, size)\n</code></pre>"},{"location":"api/traversal/","title":"<code>arcs.traversal</code>","text":"<p>Monte Carlo traversal of the reaction graph.</p>"},{"location":"api/traversal/#arcs.traversal.Traversal","title":"Traversal","text":"<pre><code>Traversal(graph, max_reaction_length=5, **kws)\n</code></pre> Source code in <code>src/arcs/traversal.py</code> <pre><code>def __init__(self, graph, max_reaction_length=5, **kws):\n    self.graph = graph\n\n    # default values:\n    self.exclude_co2 = True\n    self.max_compounds = 5\n    self.discovery_threshold = 5  # % percent\n    self.maximum_reaction_number = 10\n    self.max_steps = 5\n    self.ncpus = 4\n    self.ceiling = 2000\n    self.scale_largest = 10\n    self.rank_small_reactions_higher = True\n    self.rank_by_number_of_atoms = True\n    self.shortest_path_method = \"Djikstra\"\n    self.__dict__.update(kws)\n</code></pre>"},{"location":"api/traversal/#arcs.traversal.Traversal.graph","title":"graph  <code>instance-attribute</code>","text":"<pre><code>graph = graph\n</code></pre>"},{"location":"api/traversal/#arcs.traversal.Traversal.exclude_co2","title":"exclude_co2  <code>instance-attribute</code>","text":"<pre><code>exclude_co2 = True\n</code></pre>"},{"location":"api/traversal/#arcs.traversal.Traversal.max_compounds","title":"max_compounds  <code>instance-attribute</code>","text":"<pre><code>max_compounds = 5\n</code></pre>"},{"location":"api/traversal/#arcs.traversal.Traversal.discovery_threshold","title":"discovery_threshold  <code>instance-attribute</code>","text":"<pre><code>discovery_threshold = 5\n</code></pre>"},{"location":"api/traversal/#arcs.traversal.Traversal.maximum_reaction_number","title":"maximum_reaction_number  <code>instance-attribute</code>","text":"<pre><code>maximum_reaction_number = 10\n</code></pre>"},{"location":"api/traversal/#arcs.traversal.Traversal.max_steps","title":"max_steps  <code>instance-attribute</code>","text":"<pre><code>max_steps = 5\n</code></pre>"},{"location":"api/traversal/#arcs.traversal.Traversal.ncpus","title":"ncpus  <code>instance-attribute</code>","text":"<pre><code>ncpus = 4\n</code></pre>"},{"location":"api/traversal/#arcs.traversal.Traversal.ceiling","title":"ceiling  <code>instance-attribute</code>","text":"<pre><code>ceiling = 2000\n</code></pre>"},{"location":"api/traversal/#arcs.traversal.Traversal.scale_largest","title":"scale_largest  <code>instance-attribute</code>","text":"<pre><code>scale_largest = 10\n</code></pre>"},{"location":"api/traversal/#arcs.traversal.Traversal.rank_small_reactions_higher","title":"rank_small_reactions_higher  <code>instance-attribute</code>","text":"<pre><code>rank_small_reactions_higher = True\n</code></pre>"},{"location":"api/traversal/#arcs.traversal.Traversal.rank_by_number_of_atoms","title":"rank_by_number_of_atoms  <code>instance-attribute</code>","text":"<pre><code>rank_by_number_of_atoms = True\n</code></pre>"},{"location":"api/traversal/#arcs.traversal.Traversal.shortest_path_method","title":"shortest_path_method  <code>instance-attribute</code>","text":"<pre><code>shortest_path_method = 'Djikstra'\n</code></pre>"},{"location":"api/traversal/#arcs.traversal.Traversal.length_multiplier","title":"length_multiplier","text":"<pre><code>length_multiplier(candidate_reaction: int, **kws) -&gt; float\n</code></pre> <p>given a candidate reaction,</p> <p>if self.rank_small_reactions_higher == True, then return the sum of the coefficients as a multiplier</p> <p>i.e. H2 + 1/2 O2 = H2O has a length multiplier of 2.5</p> <p>if self.rank_by_number_of_atoms = True, then rank by the number of atoms in the reactants (which = num_atoms_in_products)</p> <p>i.e. H2 + 1/2 O2 = H2O has a length multiplier of 3</p> Source code in <code>src/arcs/traversal.py</code> <pre><code>def length_multiplier(self, candidate_reaction: int, **kws) -&gt; float:\n    \"\"\"\n    given a candidate reaction,\n\n    if self.rank_small_reactions_higher == True, then return the sum of the coefficients as a multiplier\n\n    i.e. H2 + 1/2 O2 = H2O has a length multiplier of 2.5\n\n    if self.rank_by_number_of_atoms = True, then rank by the number of atoms in the reactants (which = num_atoms_in_products)\n\n    i.e. H2 + 1/2 O2 = H2O has a length multiplier of 3\n\n    \"\"\"\n    self.__dict__.update(kws)\n\n    if self.rank_small_reactions_higher:\n        if self.rank_by_number_of_atoms:\n            reaction_dict = self.graph.nodes[candidate_reaction][\"reaction\"]\n            reactants = reaction_dict[\"reactants\"]\n            num_atoms = []\n            for species, coefficient in reactants.items():\n                num_atoms.append(\n                    np.sum(list(parse_molecule(species).values())) *\n                    coefficient\n                )\n            return np.sum(num_atoms)\n        else:\n            reaction_dict = self.graph.nodes[candidate_reaction][\"reaction\"]\n            num_reactants = np.sum(\n                list(reaction_dict[\"reactants\"].values()))\n            num_products = np.sum(list(reaction_dict[\"products\"].values()))\n            return num_reactants + num_products\n    else:\n        return 1\n</code></pre>"},{"location":"api/traversal/#arcs.traversal.Traversal.check_reactant_atoms","title":"check_reactant_atoms","text":"<pre><code>check_reactant_atoms(\n    reaction_index: int,\n    weighted_random_compounds: list,\n    **kws\n) -&gt; bool\n</code></pre> <p>takes a reaction index, chosen random_compounds, and checks for atom balance returns bool</p> Source code in <code>src/arcs/traversal.py</code> <pre><code>def check_reactant_atoms(\n    self, reaction_index: int, weighted_random_compounds: list, **kws\n) -&gt; bool:\n    \"\"\"\n    takes a reaction index, chosen random_compounds, and checks for atom balance\n    returns bool\n    \"\"\"\n    self.__dict__.update(kws)\n\n    reac_atoms = list(\n        dict.fromkeys(\n            list(\n                it.chain(*[\n                    list(parse_molecule(x))\n                    for x in self.graph.nodes()[reaction_index][\"reaction\"][\n                        \"reactants\"\n                    ]\n                ])\n            )\n        )\n    )\n    random_compounds_atoms = list(\n        dict.fromkeys(\n            list(\n                it.chain(*[\n                    list(parse_molecule(x)) for x in weighted_random_compounds\n                ])\n            )\n        )\n    )\n    if sorted(reac_atoms) == sorted(random_compounds_atoms):\n        return True\n    else:\n        return False\n</code></pre>"},{"location":"api/traversal/#arcs.traversal.Traversal.scale_large_concentrations","title":"scale_large_concentrations","text":"<pre><code>scale_large_concentrations(\n    concentrations: dict, **kws\n) -&gt; dict\n</code></pre> <p>function that takes a dict of concentrations and scales abnormally large concentrations (above ceiling DEFAULT = 3000%) and scales them by scale_highest (DEFAULT = 10% of original value)</p> <p>this is to be used with self.get_weighted_random_compounds</p> Source code in <code>src/arcs/traversal.py</code> <pre><code>def scale_large_concentrations(self, concentrations: dict, **kws) -&gt; dict:\n    \"\"\"\n    function that takes a dict of concentrations and scales abnormally large concentrations (above ceiling DEFAULT = 3000%) and scales them by scale_highest (DEFAULT = 10% of original value)\n\n    this is to be used with self.get_weighted_random_compounds\n    \"\"\"\n    self.__dict__.update(kws)\n\n    median_conc = np.median([v for v in concentrations.values() if v &gt; 0])\n    species_above_ceiling = {\n        k: v\n        for k, v in concentrations.items()\n        if v &gt; (median_conc * (1 + (self.ceiling / 100)))\n    }\n    # modify the ceiling by scaling it down to a suitable value\n    # should still max out if concentrations become way to high\n    for k, v in species_above_ceiling.items():\n        concentrations[k] = v * 1 / self.scale_largest\n\n    return concentrations\n</code></pre>"},{"location":"api/traversal/#arcs.traversal.Traversal.get_weighted_random_compounds","title":"get_weighted_random_compounds","text":"<pre><code>get_weighted_random_compounds(\n    concentrations: dict, **kws\n) -&gt; list\n</code></pre> <p>given a dictionary of concentrations e.g. {'H2O':100,'NO2':50} a weighted ranking can be returned with probabilities given a discovery threshold DEFAULT = 5%.</p> <p>exceedingly large concentrations (up to ceiling % DEFAULT = 1000% above the median concentration) that may occur are scaled using self.scale_large_concentrations (scaled with scale_largest DEFAULT = 10% of original value) such that reactions may continue even with very large concentrations of species up to a point.</p> <p>returns a list with length up to max_compounds depending on the discovery_threshold and scale_largest factors.</p> <p>CO2 is by default excluded (exclude_co2 = True) as it is considered background, however this can be turned on if you want to test CO2 containing reactions.</p> Source code in <code>src/arcs/traversal.py</code> <pre><code>def get_weighted_random_compounds(self, concentrations: dict, **kws) -&gt; list:\n    \"\"\"\n    given a dictionary of concentrations e.g. {'H2O':100,'NO2':50} a weighted ranking can be returned with probabilities given a discovery threshold DEFAULT = 5%.\n\n    exceedingly large concentrations (up to ceiling % DEFAULT = 1000% above the median concentration) that may occur are scaled using self.scale_large_concentrations (scaled with scale_largest DEFAULT = 10% of original value) such that reactions may continue even with very large concentrations of species up to a point.\n\n    returns a list with length up to max_compounds depending on the discovery_threshold and scale_largest factors.\n\n    CO2 is by default excluded (exclude_co2 = True) as it is considered background, however this can be turned on if you want to test CO2 containing reactions.\n    \"\"\"\n    self.__dict__.update(kws)\n\n    concs = copy.deepcopy(concentrations)\n\n    if self.exclude_co2 and \"CO2\" in concs:\n        # CO2 will always be too large as it is the background\n        del concs[\"CO2\"]\n\n    # scale potential large concentrations\n    concs = self.scale_large_concentrations(\n        concentrations=concs, scale_largest=self.scale_largest, ceiling=self.ceiling\n    )\n\n    # get the probabilities based upon relative concentrations:\n    p_1 = {k: v / sum(concs.values()) for k, v in concs.items()}\n    # now filter based upon the probability threshold: (discovery)\n    p_2 = {k: v for k, v in p_1.items() if v &gt;=\n           self.discovery_threshold / 100}\n    if not p_2:\n        return []\n    # remake the probabilities\n    p_3 = {k: v / sum(p_2.values()) for k, v in p_2.items()}\n    # make a list of choices based upon the probabilities\n    available = list(\n        np.random.choice(a=list(p_3), size=len(\n            concs) * 10, p=list(p_3.values()))\n    )\n    # \u00a0now make a list max_compounds long of random choices based on available\n    choices = {}\n    for i in range(self.max_compounds):\n        try:\n            compound = np.random.choice(available)\n            choices[compound] = p_3[compound]\n\n            available = list(\n                filter(lambda a: a != list(choices)[i - 1], available))\n        except ValueError:\n            pass\n    return list(choices)[0: np.random.randint(2, self.max_compounds)]\n</code></pre>"},{"location":"api/traversal/#arcs.traversal.Traversal.get_weighted_reaction_rankings","title":"get_weighted_reaction_rankings","text":"<pre><code>get_weighted_reaction_rankings(\n    weighted_random_compounds: list, **kws\n) -&gt; dict\n</code></pre> <p>returns a dictionary of {:} given a list of weighted_random_compounds from self.get_weighted_random_compounds (needs at least 2 to give a result) <p>algorithm follows: given weighted_random_compounds = ['NO2','H2O','O2'] 1. generates combinations of useable compounds     i.e. [['NO2','H2O'],['NO2','O2'],['H2O','O2']] 2. for each combination, generate a list of shortest paths using networkx.shortest_paths.all_shortest_paths 3. check that the reactant</p> Source code in <code>src/arcs/traversal.py</code> <pre><code>def get_weighted_reaction_rankings(self, weighted_random_compounds: list, **kws) -&gt; dict:\n    \"\"\"\n    returns a dictionary of {&lt;reaction_index&gt;:&lt;weighting&gt;} given a list of weighted_random_compounds from self.get_weighted_random_compounds (needs at least 2 to give a result)\n\n    algorithm follows:\n    given weighted_random_compounds = ['NO2','H2O','O2']\n    1. generates combinations of useable compounds\n        i.e. [['NO2','H2O'],['NO2','O2'],['H2O','O2']]\n    2. for each combination, generate a list of shortest paths using networkx.shortest_paths.all_shortest_paths\n    3. check that the reactant\n\n    \"\"\"\n    self.__dict__.update(kws)\n\n    # return None if there isn't enough to make a reaction\n    if len(weighted_random_compounds) &lt;= 1:\n        return None\n    # 1. generate possible combinations\n    combinations = list(it.combinations(weighted_random_compounds, 2))\n    # 2. generate shortest path possibilities from the combinations\n    possibilities = []\n    for compounds in combinations:\n        possibilities.extend([\n            x[1]\n            for x in list(\n                nx.shortest_paths.all_shortest_paths(\n                    G=self.graph,\n                    source=compounds[0],\n                    target=compounds[1],\n                    method=self.shortest_path_method,\n                )\n            )\n        ])\n\n    # 3. check that all reactant atoms (=product atoms) are accounted for.\n    # this is so that self.get_chempy_equilibrium_concentrations gives a result.\n    possibilities = [\n        i\n        for i in possibilities\n        if self.check_reactant_atoms(\n            reaction_index=i,\n            weighted_random_compounds=weighted_random_compounds,\n            **kws,\n        )\n    ]\n    # 4. rank the possibilities based on edge weight and a length_multiplier\n    # the length_multiplier is based on number of reaction coefficients\n    # idea: perhaps by number of atoms ?\n    rankings = {}\n    for i, reaction in enumerate(possibilities):\n        for compound in weighted_random_compounds:\n            try:\n                weight = self.graph.get_edge_data(u=compound, v=reaction)[0][\n                    \"weight\"\n                ] * self.length_multiplier(reaction, **kws)\n            except TypeError:\n                pass\n        rankings[reaction] = (\n            weight  # this should be ammended to return None as well\n        )\n\n    rankings = dict(\n        sorted(rankings.items(), key=lambda item: item[1])[\n            0: self.maximum_reaction_number\n        ]\n    )\n\n    return rankings\n</code></pre>"},{"location":"api/traversal/#arcs.traversal.Traversal.choose_reaction","title":"choose_reaction  <code>staticmethod</code>","text":"<pre><code>choose_reaction(ranked_reactions: dict) -&gt; int\n</code></pre> <p>given a dictionary of ranked reactions from self.get_weighted_reaction_rankings chose a reaction based on weights and probabilities</p> Source code in <code>src/arcs/traversal.py</code> <pre><code>@staticmethod\ndef choose_reaction(ranked_reactions: dict) -&gt; int:\n    \"\"\"\n    given a dictionary of ranked reactions from self.get_weighted_reaction_rankings\n    chose a reaction based on weights and probabilities\n    \"\"\"\n    weights = {\n        k: 1 / v**2 for k, v in ranked_reactions.items()\n    }  # here higher is better\n    # added a square multiplier to force more the larger coefficients\n    probabilities = {k: v / sum(weights.values())\n                     for k, v in weights.items()}\n    chosen_reaction = np.random.choice(\n        [\n            np.random.choice(\n                a=list(probabilities.keys()),\n                size=len(probabilities) * 10,\n                p=list(probabilities.values()),\n            )\n        ][0]\n    )\n    return chosen_reaction\n</code></pre>"},{"location":"api/traversal/#arcs.traversal.Traversal.generate_chempy_eqsystem","title":"generate_chempy_eqsystem","text":"<pre><code>generate_chempy_eqsystem(index: int) -&gt; EqSystem\n</code></pre> <p>given a reaction index form a chempy.equilibria.EqSystem</p> <p>eventually this will be deprecated as it is a speed bottleneck</p> <p>needs to involve charged species</p> Source code in <code>src/arcs/traversal.py</code> <pre><code>def generate_chempy_eqsystem(self, index: int) -&gt; EqSystem:\n    \"\"\"\n    given a reaction index form a chempy.equilibria.EqSystem\n\n    eventually this will be deprecated as it is a speed bottleneck\n\n    needs to involve charged species\n\n    \"\"\"\n    node_dict = self.graph.nodes[index]\n    reactants = node_dict[\"reaction\"][\"reactants\"]\n    products = node_dict[\"reaction\"][\"products\"]\n    k = node_dict[\"equilibrium_constant\"]\n\n    substances = {}\n    for compound in list(it.chain(*[list(reactants) + list(products)])):\n        substances[compound] = Substance.from_formula(\n            compound, **{\"charge\": 0})\n\n    equation = Equilibrium(reac=reactants, prod=products, param=k)\n    try:\n        return EqSystem([equation], substances=substances)\n    except Exception:\n        return None\n</code></pre>"},{"location":"api/traversal/#arcs.traversal.Traversal.chempy_equilibrium_concentrations","title":"chempy_equilibrium_concentrations  <code>staticmethod</code>","text":"<pre><code>chempy_equilibrium_concentrations(\n    concentrations: dict,\n    equilibrium_reaction: EqSystem,\n    chempy_sane=True,\n) -&gt; dict\n</code></pre> <p>generate equilibrium concentrations if the reaction is \"sane\" and a \"success\" then it returns the equilibrium concentrations as a dict of concentrations elsewise return None</p> Source code in <code>src/arcs/traversal.py</code> <pre><code>@staticmethod\ndef chempy_equilibrium_concentrations(\n    concentrations: dict,\n    equilibrium_reaction: EqSystem,\n    chempy_sane=True,\n) -&gt; dict:\n    \"\"\"\n    generate equilibrium concentrations\n    if the reaction is \"sane\" and a \"success\" then it returns the equilibrium concentrations as a dict of concentrations\n    elsewise\n    return None\n    \"\"\"\n\n    warnings.simplefilter(\"ignore\")\n    _concs = copy.deepcopy(concentrations)\n    try:\n        result = equilibrium_reaction.solve(init_concs=_concs)\n        # assert result.success and result.sane\n        if chempy_sane:\n            assert result.success and result.sane\n        else:\n            assert result.success\n        for compound, concentration in enumerate(result.conc):\n            _concs[equilibrium_reaction.substance_names()[compound]\n                   ] = concentration\n        return _concs\n    except Exception:\n        return None\n</code></pre>"},{"location":"api/traversal/#arcs.traversal.Traversal.random_walk","title":"random_walk","text":"<pre><code>random_walk(\n    initial_concentrations: dict, chempy_sane=True, **kws\n) -&gt; dict\n</code></pre> <p>does a random sampling of the reaction network with max_steps  DEFAULT = 10.</p> <p>for each sample step: 1. get weighted random compounds 2. get ranked reactions 3. choose a reaction 3. generate a chempy eqsystem 4. calculate the equilibrium concentrations 5. update the concentrations and reaction statistics</p> Source code in <code>src/arcs/traversal.py</code> <pre><code>def random_walk(\n    self,\n    initial_concentrations: dict,\n    chempy_sane=True,  # typically for very large\n    ** kws,\n) -&gt; dict:\n    \"\"\"\n    does a random sampling of the reaction network with max_steps  DEFAULT = 10.\n\n    for each sample step:\n    1. get weighted random compounds\n    2. get ranked reactions\n    3. choose a reaction\n    3. generate a chempy eqsystem\n    4. calculate the equilibrium concentrations\n    5. update the concentrations and reaction statistics\n    \"\"\"\n    self.__dict__.update(kws)\n\n    concentrations = {0: initial_concentrations}\n    reactionstats = {0: None}\n    i = 0\n    for step in range(1, self.max_steps + 1):\n        _concentrations = copy.deepcopy(concentrations[i])\n        # 1 grab weighted_random_compounds\n        weighted_random_compounds = self.get_weighted_random_compounds(\n            concentrations=_concentrations,\n            exclude_co2=self.exclude_co2,\n            max_compounds=self.max_compounds,\n            discovery_threshold=self.discovery_threshold,\n            scale_largest=self.scale_largest,\n            ceiling=self.ceiling,\n            **kws,\n        )\n        # 2 grab reaction rankings\n        ranked_reactions = self.get_weighted_reaction_rankings(\n            weighted_random_compounds=weighted_random_compounds, **kws\n        )\n        # 3 if no reactions found then break the for loop\n        if not ranked_reactions:\n            break\n\n        # 4 choose a reaction\n        chosen_reaction_index = self.choose_reaction(\n            ranked_reactions=ranked_reactions\n        )\n        # 5 generate a chempy eqsystem\n        eqsystem = self.generate_chempy_eqsystem(\n            index=chosen_reaction_index)\n\n        # 6 get equilibrium_concentrations and update relevant dictionaries.\n        final_concentrations = self.chempy_equilibrium_concentrations(\n            concentrations=_concentrations,\n            equilibrium_reaction=eqsystem,\n            chempy_sane=chempy_sane,\n        )\n\n        if final_concentrations:\n            i += 1\n            concentrations[i] = final_concentrations\n            try:\n                reactionstats[i] = {\n                    \"reaction\": self.graph.nodes[chosen_reaction_index][\"reaction\"],\n                    \"equilibrium_constant\": self.graph.nodes[chosen_reaction_index][\n                        \"equilibrium_constant\"\n                    ],\n                }\n            except KeyError:\n                reactionstats[i] = {\n                    \"reaction\": self.graph.nodes[chosen_reaction_index][\"reaction\"],\n                    \"log_equilibrium_constant\": self.graph.nodes[chosen_reaction_index][\n                        \"log_equilibrium_constant\"\n                    ],\n                }\n\n    return {\"concentrations\": concentrations, \"reaction_statistics\": reactionstats}\n</code></pre>"},{"location":"api/traversal/#arcs.traversal.Traversal.sampling_function","title":"sampling_function","text":"<pre><code>sampling_function(iterable, **kws)\n</code></pre> <p>sampling function to be multiprocessed - runs one random walk</p> Source code in <code>src/arcs/traversal.py</code> <pre><code>def sampling_function(self, iterable, **kws):\n    \"\"\"\n    sampling function to be multiprocessed - runs one random walk\n    \"\"\"\n    self.__dict__.update(kws)\n    initial_concentrations = self.initial_concentrations\n    return self.random_walk(initial_concentrations=initial_concentrations, **kws)\n</code></pre>"},{"location":"api/traversal/#arcs.traversal.Traversal.sample","title":"sample","text":"<pre><code>sample(\n    initial_concentrations: dict,\n    nsamples: int,\n    ncpus: int,\n    tqdm_kws: dict = {},\n    **kws\n) -&gt; dict\n</code></pre> <p>samples the graph network nsamples DEFAULT = 1000 multiprocessed with ncpus DEFAULT = 4</p> Source code in <code>src/arcs/traversal.py</code> <pre><code>def sample(\n    self,\n    initial_concentrations: dict,\n    nsamples: int,\n    ncpus: int,\n    tqdm_kws: dict = {},\n    **kws,\n) -&gt; dict:\n    \"\"\"\n    samples the graph network nsamples DEFAULT = 1000\n    multiprocessed with ncpus DEFAULT = 4\n    \"\"\"\n    self.__dict__.update(kws)\n    self.initial_concentrations = initial_concentrations\n\n    data = tqdm_pathos.map(\n        self.sampling_function,\n        list(range(nsamples)),\n        n_cpus=ncpus,\n        tqdm_kwargs=tqdm_kws,\n        **kws,\n    )\n\n    return data\n</code></pre>"},{"location":"guide/analysis/","title":"Analysing results","text":""},{"location":"guide/building-the-graph/","title":"Building the reaction graph","text":""},{"location":"guide/concentrations/","title":"Initial concentrations","text":""},{"location":"guide/how-it-works/","title":"How ARCS works","text":""},{"location":"guide/sampling/","title":"Sampling the network","text":""}]}