# `arcs.errors`

!!! warning "Work in progress"
    `arcs.errors` computes error metrics by comparing the bundled quantum data
    against the Burcat thermochemical database. It is not yet wired into the main
    ARCS pipeline and its interface may change.

::: arcs.errors.RMSEErrors
